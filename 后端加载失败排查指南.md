# 后端加载失败排查指南

## 🔍 问题现象
启动程序后显示："后端加载失败" 或 "无法连接到后端"

---

## ✅ 解决方案

### 方案1：检查Python是否安装

**操作步骤**：
1. 按 `Win + R`，输入 `cmd`，回车
2. 在黑色窗口中输入：`python --version`
3. 按回车

**结果判断**：
- ✅ 如果显示版本号（如 `Python 3.9.0`），说明Python已安装
- ❌ 如果显示"不是内部或外部命令"，说明Python未安装或不在PATH中

**解决方法**：
1. 访问 https://www.python.org/downloads/
2. 下载最新版Python（3.8+）
3. **安装时务必勾选 "Add Python to PATH"**（重要！）
4. 安装完成后重启电脑

---

### 方案2：安装Python依赖包

**操作步骤**：
1. 双击运行 `安装Python依赖.cmd`（本目录下的脚本）
2. 等待安装完成
3. 重启量化回测平台

**手动安装**（如果脚本失败）：
```bash
pip install backtrader pandas numpy tushare
```

---

### 方案3：检查防火墙

**问题**：防火墙阻止了后端进程

**解决方法**：
1. Windows安全中心 -> 防火墙和网络保护 -> 允许应用通过防火墙
2. 找到"Python"或"量化回测平台"，勾选"专用"和"公用"
3. 确定保存

**临时方法**（测试用）：
关闭防火墙后再启动程序

---

### 方案4：端口被占用

**问题**：默认端口5000被其他程序占用

**解决方法**：
1. 打开 `backend\main.py`
2. 找到 `app.run(port=5000)`
3. 改成其他端口（如5001、5002）
4. 重启程序

**或者杀死占用端口的进程**：
```bash
netstat -ano | findstr :5000
taskkill /PID <进程号> /F
```

---

### 方案5：Tushare token未配置

**问题**：后端启动但无法获取数据

**解决方法**：
1. 启动程序
2. 点击"设置"
3. 输入Tushare token
4. 保存

**获取token**：
- 访问 https://tushare.pro/register
- 注册并登录
- 在个人中心复制token

---

## 🐛 高级排查

### 查看后端日志

**方法1：从程序查看**
1. 启动程序
2. 按 `F12` 打开开发者工具
3. 查看Console标签页的错误信息

**方法2：手动启动后端**
1. 打开命令提示符
2. `cd` 到程序安装目录的 `resources\backend`
3. 运行：`python main.py`
4. 查看错误信息

---

### 重新打包后端

如果以上方法都不行，可能是后端文件损坏：

1. 打开安装目录
2. 进入 `resources\backend`
3. 检查是否有 `main.py` 和其他Python文件
4. 如果文件缺失，需要重新打包

---

## 📞 紧急联系方式

如果以上方法都无法解决，请：
1. 截图错误信息
2. 记录Python版本（`python --version`）
3. 记录操作系统版本（Win10/Win11）
4. 联系开发者

---

## 📝 预防措施

为避免后续出现问题，建议：
1. 安装Python时勾选"Add Python to PATH"
2. 安装完成后重启电脑
3. 运行 `安装Python依赖.cmd` 确保依赖完整
4. 配置Tushare token后再使用

---

**祝安装顺利！🎉**

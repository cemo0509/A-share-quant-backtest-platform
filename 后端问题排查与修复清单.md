# A股量化回测平台 — 后端问题排查与修复清单

> 整理时间：2026-07-09
> 状态：端口启动问题已修复并验证可用；指标 500 错误已定位根因，修复代码已落地待重新打包验证。

---

## 一、问题总览

| 序号 | 现象 | 类型 | 状态 |
|------|------|------|------|
| 1 | `net::ERR_CONNECTION_REFUSED`（前端连不上后端） | 端口/进程问题 | ✅ 已修复并验证 |
| 2 | `GET /api/stocks/indicators?symbol=sh600276` 返回 `500 (Internal Server Error)` | 后端接口逻辑 bug | 🔧 已定位+改代码，待打包验证 |

---

## 二、问题 1：前端连不上后端（ERR_CONNECTION_REFUSED）

### 根因
1. **僵尸后端进程**：强制结束应用时，Python 后端进程未退出，一直独占 8000 端口，新启动的后端拿不到端口。
2. **重试机制失效**：`uvicorn.run()` 端口绑定失败时走 `sys.exit(1)`（抛 `SystemExit`），而原代码只 `except Exception`，捕获不到 `SystemExit` → 重试从未触发。
3. **重试时长不足**：原逻辑仅 5×2s=10s，远小于 Windows `TIME_WAIT`（约 120s）。
4. **前端无重试**：axios 一启动就发请求，后端晚几秒就绪即永久报错，需手动刷新。

### 已应用的修复
- `backend/main.py`：改用 `uvicorn.Config + Server.run()`，捕获 `SystemExit`；重试次数 `1→30`（30×4s 覆盖 TIME_WAIT）。
- `electron/main.js`：启动前杀掉所有 `python.exe/pythonw.exe` + 等待 5s；退出时彻底清理所有 Python 进程；spawn 时把嵌入式 Python 目录加入 PATH。
- `frontend/src/api/index.ts`：给 axios 加连接失败自动重试（最多 30 次、指数退避）。

---

## 三、问题 2：指标接口返回 500（当前重点）

### 报错原文
```
GET /api/stocks/indicators -> 500
ValueError: Out of range float values are not JSON compliant
```

### 完整错误栈（关键行）
```
File "core/indicators.py", line 215, in get_indicators
    indicators = calc_all_indicators(df)
...
File "json/encoder.py", line 258, in iterencode
ValueError: Out of range float values are not JSON compliant
```

### 根因
1. AKShare 行情接口拉取失败（网络/限流），`data_loader` 自动降级为**模拟 K 线数据**（本次复现 `600276` 生成 397 条模拟数据）。
2. 模拟数据 + 指标窗口不足，导致 `calc_all_indicators` 计算出的部分指标（CCI、WR、BIAS、RSI 前段等）出现 **NaN / Infinity**。
3. Starlette 的 `JSONResponse.render` 使用 `json.dumps(..., allow_nan=False)`，遇到非有限浮点直接抛 `ValueError`，响应阶段崩溃 → 整个接口 500。

### 触发条件
- 任意指标接口（indicators / signals / 部分 strategy 计算）在数据为**模拟数据**或**数据量不足一个指标窗口**时，都会产生 NaN，从而 500。

---

## 四、问题 2 的修复方案（代码已落地，待验证）

### 修复 A：`backend/core/indicators.py`
新增 `_clean_nan()` 递归清洗函数，把 NaN/Inf 转成 `None`，并在 `calc_all_indicators` 返回前调用：

```python
import math
import numpy as np

def _clean_nan(obj):
    """递归把 NaN/Inf 清洗成 None，避免 json.dumps 报 'Out of range float values'。"""
    if isinstance(obj, dict):
        return {k: _clean_nan(v) for k, v in obj.items()}
    if isinstance(obj, (list, tuple)):
        return [_clean_nan(v) for v in obj]
    if isinstance(obj, (float, np.floating)):
        f = float(obj)
        if math.isnan(f) or math.isinf(f):
            return None
        return f
    if isinstance(obj, (np.integer,)):
        return int(obj)
    return obj

# 在 calc_all_indicators 末尾：
return _clean_nan(indicators)
```

### 修复 B：`backend/main.py`（全局防线）
拦截 Starlette 的 JSON 序列化，统一把非有限浮点转为 `None`，覆盖所有接口（不仅限于 indicators）：

```python
import math as _math
import numpy as _np
import json as _json

_original_json_dumps = _json.dumps

def _safe_json_dumps(*args, **kwargs):
    kwargs.setdefault("allow_nan", False)
    def _default(o):
        if isinstance(o, float) and (_math.isnan(o) or _math.isinf(o)):
            return None
        if isinstance(o, _np.floating):
            f = float(o)
            return None if (_math.isnan(f) or _math.isinf(f)) else f
        if isinstance(o, _np.integer):
            return int(o)
        if isinstance(o, _np.ndarray):
            return o.tolist()
        raise TypeError(...)
    kwargs["default"] = _default
    return _original_json_dumps(*args, **kwargs)

import starlette.responses as _sr
_sr.json.dumps = _safe_json_dumps

# 覆盖 JSONResponse.render（注意：fastapi.responses 没有 json 属性，不要写 _fr.json.dumps）
class SafeJSONResponse(JSONResponse):
    def render(self, content) -> bytes:
        return _safe_json_dumps(content, ensure_ascii=False, allow_nan=False).encode("utf-8")

JSONResponse = SafeJSONResponse
```

> ⚠️ 易错点：`fastapi.responses` 模块**没有** `json` 属性（只有 `ujson`）。不要执行 `fastapi.responses.json.dumps = ...`，会抛 `AttributeError: module 'fastapi.responses' has no attribute 'json'`，导致后端启动直接崩溃。只改 `starlette.responses.json.dumps` 并覆盖 `JSONResponse.render` 即可。

---

## 五、验证步骤（交给 code 执行）

1. 杀掉所有残留进程：
   ```powershell
   taskkill /f /im python.exe; taskkill /f /im "A股量化回测平台.exe"; taskkill /f /im electron.exe
   ```
2. 用源码环境启动后端（源码路径 `backend/main.py`，Python 用 `python-embed\python.exe`）：
   ```powershell
   $env:PATH = "项目根\python-embed;" + $env:PATH
   python-embed\python.exe -u backend\main.py
   ```
3. 等待约 10s 后请求曾报 500 的接口：
   ```powershell
   Invoke-WebRequest "http://127.0.0.1:8000/api/stocks/indicators?symbol=sh600276" -UseBasicParsing
   ```
   - 预期：返回 `200` 且响应体为合法 JSON（指标数组中的 NaN 位置变为 `null`）。
4. 顺带验证相关接口不 500：
   - `/api/stocks/signals?symbol=sh600276&strategy=macd`
   - `/api/stocks/kline?symbol=sh600276`
5. 确认 `pkg_be_err.log` 中**不再出现** `ValueError: Out of range float values are not JSON compliant`。

---

## 六、重新打包

验证通过后重新打包（前端+后端都已改）：
1. `cd frontend && npm run build`
2. `Copy-Item frontend\dist\* electron\dist -Recurse -Force`
3. `npm run dist`（输出目录 `dist_build`，已在 `electron-builder.yml` 配置）

打包完成后安装包位置：
```
项目根\dist_build\A股量化回测平台 Setup 1.1.2.exe
```

---

## 七、给用户的安装建议
1. 彻底退出旧程序（结束 `A股量化回测平台.exe` / `electron.exe` / `python.exe`）。
2. 删除旧安装目录（如 `C:\Users\22864\Desktop\量化软件\1.1.2\quant-a-stock`）。
3. 安装到新目录，避免文件锁。
4. 正常使用菜单退出，避免强制结束产生僵尸进程。

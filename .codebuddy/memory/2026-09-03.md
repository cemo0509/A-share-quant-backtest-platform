# 2026-09-03 工作日志

## 三处 P0 修复（WorkBuddy 试用报告之后）

WorkBuddy 实测报告指出三处 P0，已全部修复并验证
（commit `36cd9a4`，3 文件 +82/-17）。

### A-01：可视化编辑器会生成「有钱就买」的策略（最严重）

- **根因**：`backend/core/visual_editor/codegen.py` 在无法编译的节点处**静默退化**
  - `_target_expr`（385 行）返回 `"0.0"` → 生成 `MA5 > 0.0` → **恒真**（价格恒 > 0）
    → 策略变成"有钱就买"，照样产出完整指标、资金曲线、交易明细，`status: ok`
  - `_compile_leaf` / `_compile_golden_cross` / `_compile_node` 返回 `("False","False")`
- **入口原检查只能抓"全部失效"**：`if buy_expr in ("False", "(False)")`
  - 双条件其一失效 → `(x>0) and False` → 抓不到 ❌
  - 目标取 0 导致恒真 → `(ma5[0] > 0.0)` → 抓不到 ❌
- **修复**（约 25 行）：
  - `_CodegenContext` 新增 `self.degraded: list[str] = []`
  - 6 个退化点全部登记原因（仍返回占位值保证语法完整）
  - `generate_strategy_code` 入口统一检查：非空即 `raise ValueError`，
    列出所有退化原因，明确"为避免生成看似正常实则错误的策略，已拒绝生成"

### B-01 / B-02：导出（**重要更正，必须记住**）

- WorkBuddy 测的是 `export.py`（挂在 `/api/export`）。但
  **前端实际调用的是 `optimize.py` 的 `/export/json`、`/export/csv`**（372/390 行）！
- `optimize.py` 那套导出**是正常工作的**：用 `ExportJsonRequest`(result: dict) /
  `ExportCsvRequest`(trades: list[dict])，文件名由后端生成 ASCII 时间戳，还有空交易 400 检查。
- 所以"中文文件名必定 500""CSV 从来没能用过"**对实际使用并不成立**（那是死代码里的坑）。
- 但 `export.py` 的缺陷真实存在，已修（死代码将来谁调用谁踩）：
  - **B-01**：新增 `_content_disposition()`，按 **RFC 5987** 输出
    `filename="<ascii回退>"; filename*=UTF-8''<percent-encoded>`
  - **B-02**：新增 `ExportCsvDataRequest`（`data: list[dict]`），
    与 `ExportRequest`（`data: dict`）分开——原实现"传 dict 报要 list、传 list 过不了 dict 校验"
    是死结，CSV 导出从未真正可用
  - 错误信息：500 带真实原因（不再"请稍后重试"）；空列表返回 400

## 验证（对照实验，不只信代码）

临时脚本 `backend/_verify_fixes.py`（已删除，下划线开头被 gitignore 忽略）：

| 场景 | 修复前 | 修复后 |
|---|---|---|
| [1] 合法条件 | 生成成功 | ✅ 生成成功（1631 字符，未误伤） |
| [2] 单条件失效 | 报错 | ✅ 正确拦截 |
| [3] 双条件其一失效 | 生成 `(x>0) and False` | ✅ **正确拦截** |
| [4] MA5>不存在指标 | 生成 `MA5 > 0.0` 恒真 | ✅ **正确拦截**（提示"若取 0 会使条件恒真"） |
| JSON/CSV 中文文件名 | 500 | ✅ **200** |
| CSV 传 list | 死结（永远不可能成功） | ✅ **200** |
| CSV 空列表 | 笼统 500 | ✅ **400** 明确提示 |

`pytest backend/tests` → **30 passed**（未破坏既有功能）

## WorkBuddy 提炼的判断标准（值得长期遵循）

> 降级发生在"锦上添花"的环节，还是"结论本身"的环节？
> 判断：如果这个降级悄悄发生了，你拿到的数字还能不能用来做决策？
> **能 → 降级；不能 → 必须报错。**

- 沪深300 拉不到 → 降级为 None ✅（结论主体"策略指标"未被污染）
- 买入条件编译失败 → 生成恒真 ❌（结论主体被悄悄替换）

## 待办
- 重新打包（后端代码变更）
- token 轮换（用户自行决定）

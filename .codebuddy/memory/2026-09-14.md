# 2026-09-14 工作日志

## T3：自定义因子选股（完成，待用户白天实测）

用户要求"顺手做掉 T3"。已完成（commit `4b35b31`，8 文件 +694/-1）。

### 后端筛选引擎 `backend/core/screener.py`（341 行）
- 直接对本地 K 线 DataFrame **计算指标并布尔判断，不跑 backtrader 回测**（这是"秒级"的基础）
- 指标（pandas 向量化）：MA / EMA / MACD / KDJ / BOLL / RSI / CCI / WR / BIAS / OBV /
  成交量 / 均量 / 量比 / 成交额 / 换手 / 价格 / 振幅 / 涨跌幅 / 新高新低 / 均线排列
- 关键防护（同 A-01 原则，宁缺毋滥）：
  - mock 数据（`is_mock`）一律跳过——随机行情筛出的股票毫无参考价值
  - 无法计算的指标/运算符 → 该股不满足（不让条件静默变"恒真"）

### 后端 API `backend/api/screener.py`（POST `/api/screener/run`）
- 提取条件叶子（递归拍平嵌套 group）、解析日期/股票范围（复用 `_get_scan_symbols`）、
  可选 `prefetch_klines` 预热（首次下载慢、之后改条件秒级）、`screen_symbols` 筛选
- `schemas` 加 `ScreenerRequest`；`main.py` 注册路由

### 前端 `pages/FactorScreener.tsx`（148 行）
- FactorEditor 组合因子 + 范围（沪深300/中证500/全市场）+ 日期区间 + 开始筛选 + 结果表格
- `App.tsx` 接入路由 `/screener` + 「选股与行情」菜单；`api/index.ts` 加 `runScreener`

### 验证（端到端冒烟，不只信语法）
- 恒真条件 `price>0` → 返回 3/3（贵州茅台/平安银行/美的集团，**真实数据非 mock**）✅
- `MA5>0`=3 且 `MA5<MA10`=3（与 `MA5>MA10`=0 完美互补）→ **指标计算正确** ✅
- `pytest` 30 passed ✅
- 前端 tsc 通过、后端 py_compile 通过

### push 受阻（403，token 失效）
- 本地 commit `4b35b31` 已保住
- 用户需在自己终端重新认证后补 push
  （`917c067` 策略编辑器改造 + `4b35b31` 因子选股 两个 commit 待推）

### 待办
- 打包（进行中）
- 用户认证后补 push
- T5（可选）：指标宽表预计算，进一步提速到毫秒级

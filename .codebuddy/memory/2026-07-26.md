# 2026-07-26 工作日志

## 多数据源接入（通达信 + 雪球）
- 新增 `backend/core/datasource.py`：数据源抽象层，统一输出「东方财富风格」字段，前端无需改动
- 三种源：东方财富(akshare, 默认) / 通达信(pytdx, 直连行情服务器) / 雪球(HTTP)
- 切换方式：环境变量 `QUANT_DATASOURCE`(eastmoney/tongdaxin/xueqiu) 或运行时 `set_active_source()`
- 失败自动降级到东方财富；雪球不支持快照/分钟K线，这两类请求自动回退东财
- 重构 `fetch_realtime_quote`/`fetch_minute_kline`(data_loader.py) 和 `get_spot_snapshot`(filters.py) 路由到 datasource
- 新增 akshare 辅助函数 `_fetch_minute_from_akshare` 供东财源复用
- API 端点：`GET/POST /api/data/source`（查看/切换）
- 前端 `RealtimeQuotes.tsx` 增加数据源切换下拉框；`api/index.ts` + `services/api.ts` 导出接口
- `requirements.txt` 新增 `pytdx`、`requests`
- 验证：pytest 3/3 通过；tsc 零错误；vite 构建成功；API 端点 TestClient 验证通过

## 云端仓库
- GitHub: https://github.com/cemo0509/A-share-quant-backtest-platform （用户 PAT: ghp_ 开头）
- 分支: main

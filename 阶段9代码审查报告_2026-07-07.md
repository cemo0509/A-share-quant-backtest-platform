# 阶段9 代码审查报告

**日期**: 2026-07-07  
**审查范围**: 全项目代码（backend + electron + frontend 结构）  
**审查类型**: 安全、性能、代码质量、架构

---

## 一、审查概览

| 级别 | 数量 | 说明 |
|------|------|------|
| 🔴 高风险 | 4 | 需立即修复 |
| 🟡 中风险 | 8 | 建议近期修复 |
| 🟢 低风险 | 8 | 可后续优化 |
| **合计** | **20** | |

---

## 二、🔴 高风险问题

### H1. 自定义策略代码加载存在沙箱逃逸风险

**文件**: `backend/core/strategies/custom_manager.py:259-317`  
**问题**: `load_strategy_from_code()` 和 `load_custom_strategy_class()` 虽然做了安全检查（`_validate_code_security`），但最终还是通过 `importlib.util` + `exec_module` 执行用户代码。目前的禁止列表不完整，攻击者仍可能通过以下方式绕过：
- 使用 `__builtins__['exec']` 或 `getattr(__builtins__, 'ex'+'ec')` 绕过字符串匹配
- 使用 base64 解码执行恶意代码
- 利用 pickle/marshal 反序列化

**建议**:
```python
# 增加更严格的安全检查
FORBIDDEN_PATTERNS += [
    r'__builtins__',
    r'base64',
    r'codecs',
    r'\.decode\s*\(',  # 字符串解码
    r'chr\s*\(',       # 字符构造
    r'ord\s*\(',       # 字符转码
]

# 同时限制可用内置函数（使用受限的 __builtins__）
# 在 exec_module 前注入受限环境
module.__builtins__ = {
    k: v for k, v in __builtins__.__dict__.items()
    if k not in {'exec', 'eval', 'compile', '__import__', 'open', 'input'}
}
```

### H2. Electron 中 shell.openExternal 可能被恶意利用

**文件**: `electron/main.js:67-69`  
**问题**: `shell.openExternal` 打开的是用户提供的 URL（虽然当前是硬编码的清华镜像站和 Python 官网），但如果有任何动态构造 URL 的场景，可能导致命令注入。

**当前风险等级**: 当前使用硬编码 URL，实际风险较低。但需要确保未来任何动态 URL 构造都经过严格校验。

**建议**: 对所有 `shell.openExternal` 调用的 URL 添加白名单校验。

### H3. 全局异常处理器可能掩盖真正的错误

**文件**: `backend/main.py:53-60`  
**问题**: 全局异常处理器将所有异常统一返回 "服务器内部错误"，但同时也捕获了 `asyncio.CancelledError` 等不应该被静默处理的异常。

**建议**:
```python
@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    # 不要让 CancelledError 等内部异常被静默处理
    if isinstance(exc, (asyncio.CancelledError, SystemExit, KeyboardInterrupt)):
        raise exc
    logger.error(f"未捕获异常: {request.method} {request.url.path}\n{traceback.format_exc()}")
    return JSONResponse(
        status_code=500,
        content={"status": "error", "detail": "服务器内部错误，请稍后重试"},
    )
```

### H4. 模拟数据生成中的固定随机种子影响可复现性但可能被滥用

**文件**: `backend/core/data_loader.py:188`  
**问题**: `np.random.seed(hash(symbol) % 2**32)` 固定种子用于生成模拟 K 线，这确保了同一股票的数据一致性。但如果 hash 碰撞，不同股票会生成完全相同的模拟数据。

**建议**: 当前风险较低（hash 碰撞概率极低），但建议在文档中说明这是模拟数据的特性。

---

## 三、🟡 中风险问题

### M1. `stocks.py` 中技术指标函数与 `indicators.py` 存在大量代码重复

**文件**: 
- `backend/api/stocks.py:331-414` — 内联实现 `_ema`, `_sma`, `_rolling_std`, `_rsi`, `_kdj`
- `backend/core/indicators.py` — 使用 pandas 实现相同指标

**问题**: 两处实现相同功能但使用不同算法（numpy 手动实现 vs pandas rolling/ewm），会导致：
1. 维护成本翻倍
2. 信号计算结果可能不一致
3. 代码膨胀

**建议**: 统一使用 `core/indicators.py` 中的实现，删除 `stocks.py` 中的内联函数。

### M2. `optimize.py` 中串行执行参数扫描，未充分利用并行能力

**文件**: `backend/api/optimize.py:92-133`  
**问题**: 参数优化中每个参数组合的回测是串行执行的（`for idx, combo in enumerate(param_combinations)`），对于2000组参数上限，如果每组耗时5秒，总计需要10000秒。

**建议**: 使用 `ThreadPoolExecutor` 并行执行多个参数组合，但需限制并发数（如8个worker）：
```python
from concurrent.futures import ThreadPoolExecutor, as_completed

with ThreadPoolExecutor(max_workers=min(8, len(param_combinations))) as pool:
    futures = {pool.submit(_run_single, params): params 
               for params in param_combinations}
    for future in as_completed(futures, timeout=OPTIMIZE_TOTAL_TIMEOUT):
        # 收集结果...
```

### M3. `data_loader.py` 中的实时数据缓存 Key 使用拼接字符串，可能导致内存浪费

**文件**: `backend/core/data_loader.py:387`  
**问题**: `cache_key = ",".join(sorted(symbols))` — 当不同的 `symbols` 组合（如 `["000001", "000002"]` 和 `["000002", "000001"]`）产生不同 key 时，会缓存重复数据。排序已解决这个问题，但当股票列表不同但包含重叠时，仍会缓存多份。

**建议**: 考虑使用单个股票的缓存聚合，而非按请求的符号组合缓存：
```python
# 为每只股票单独缓存，请求时聚合
for symbol in symbols:
    with _REALTIME_CACHE_LOCK:
        if symbol in _SINGLE_CACHE:
            ...
```

### M4. `trading.py` 的 `place_order` 中 ValueError 直接透传给客户端

**文件**: `backend/api/trading.py:81-82`  
**问题**: `raise HTTPException(status_code=400, detail=str(e))` — 当 `TradingSimulator.place_order` 抛出 `ValueError("持仓不足: 000001")` 时，异常消息直接返回给客户端，包含业务敏感信息。

**建议**: 
```python
except ValueError as e:
    logger.warning(f"下单参数无效: {e}")
    raise HTTPException(status_code=400, detail="下单参数无效，请检查输入")
```

### M5. `stock_scan.py` 的 `_run_single_scan` 使用 `thread.join(timeout)` 无法真正杀死超时线程

**文件**: `backend/api/stock_scan.py:455-487`  
**问题**: `thread.join(timeout=timeout)` 只是等待最多 timeout 秒，超时后返回但线程仍在运行（Python 线程无法强制终止）。这会导致：
1. 超时线程继续消耗资源
2. 最终 `as_completed` 中的 `future.result(timeout=30)` 会再次超时
3. 被遗弃的线程可能累积导致资源泄漏

**建议**: 使用 `concurrent.futures.ThreadPoolExecutor` + `future.cancel()` 替代原始 threading：
```python
with ThreadPoolExecutor(max_workers=1) as executor:
    future = executor.submit(_scan_inner, ...)
    try:
        return future.result(timeout=timeout)
    except TimeoutError:
        future.cancel()
        return None
```

### M6. `custom_manager.py` 中 `load_strategy_from_code` 创建的临时文件可能在异常时泄漏

**文件**: `backend/core/strategies/custom_manager.py:281-316`  
**问题**: 使用 `tempfile.NamedTemporaryFile(delete=False)` 创建临时文件，finally 块中调用 `os.unlink(temp_file)` 清理。如果进程在 `os.unlink` 执行前崩溃，临时文件将永久保留在磁盘上。

**建议**: 使用 `tempfile.TemporaryDirectory()` 上下文管理器或注册 `atexit` 清理：
```python
import atexit
temp_files = []

def _cleanup_temp():
    for f in temp_files:
        try: os.unlink(f)
        except: pass

atexit.register(_cleanup_temp)
```

### M7. `market_state.py` 中 `get_index_data` 的模拟数据生成每次调用都不同

**文件**: `backend/core/market_state.py:231-233`  
**问题**: 当 AKShare 失败时，`get_index_data` 返回 `np.random.normal` 生成的随机数据，每次调用结果都不同。这会导致市场状态判断结果不稳定。

**建议**: 使用固定种子或基于日期生成确定性模拟数据，确保同一日的多次调用结果一致。

### M8. `indicators.py` 中 KDJ 计算使用纯 Python 循环，性能不佳

**文件**: `backend/core/indicators.py:54-59`  
**问题**: KDJ 的递推公式用 Python for 循环实现，当数据量较大时（如1000条以上）性能较差。

**建议**: 使用 numba 加速或改用向量化实现。

---

## 四、🟢 低风险问题

### L1. 未使用的 import

**文件**: 
- `backend/api/stocks.py:5` — `import numpy as np`（实际在函数内部使用）
- `backend/api/optimize.py:5` — `import csv` 和 `import io`（实际使用，但在函数内部重复 import）
- `backend/api/export.py:5` — `import csv`（同样在函数内重复 import）

**问题**: 部分 import 在模块顶部声明但未使用，或在函数内部重复 import。

**建议**: 清理未使用的 import，确保 import 风格一致。

### L2. `stock_scan.py` 中硬编码的兜底股票列表难以维护

**文件**: `backend/api/stock_scan.py:386-413`  
**问题**: 约100只股票代码硬编码在函数中，难以维护和更新。应考虑从 `StockNameCache` 统一获取。

**建议**: 重构为从 `data/stock_names.py` 获取，仅在极端情况下使用内置列表。

### L3. `data_loader.py` 中 `_STOCK_NAME_HARDCODED` 与 `stock_names.py` 中 `_hardcoded` 重复

**文件**: 
- `backend/core/data_loader.py:246-258`
- `backend/data/stock_names.py:75-98`

**问题**: 两份硬编码股票名称映射内容高度重复，维护时容易遗漏同步更新。

**建议**: 统一使用 `data/stock_names.py` 中的 `StockNameCache` 作为唯一数据源。

### L4. FastAPI 路由缺少请求速率限制

**文件**: `backend/main.py` 及所有 API 路由  
**问题**: 没有任何速率限制机制，攻击者可以通过高频请求耗尽资源（特别是回测和参数优化接口）。

**建议**: 添加 `slowapi` 或自定义中间件实现速率限制：
```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.post("/api/backtest/run")
@limiter.limit("10/minute")
def run(req: BacktestRequest):
    ...
```

### L5. 日志级别使用不一致

**文件**: 多处  
**问题**: 部分异常使用 `logger.debug()`（如 `stock_scan.py:224`），部分使用 `logger.warning()`，导致排查问题时信息不足。

**建议**: 统一规范：网络/数据获取失败用 `warning`，业务逻辑异常用 `error`，仅调试信息用 `debug`。

### L6. `trading.py` 全局变量 `_trading_simulator` 缺少模块级文档

**文件**: `backend/api/trading.py:50-56`  
**问题**: 全局交易模拟器在模块加载时创建并从文件恢复状态，但没有文档说明其生命周期和线程安全性。

**建议**: 添加模块级文档说明 `_trading_simulator` 的使用规范。

### L7. Pydantic Schema 中部分字段缺少输入校验

**文件**: `backend/models/schemas.py:151-157`  
**问题**: `PlaceOrderRequest` 中 `action` 字段只标注 `str`，没有枚举校验（应为 `buy` 或 `sell`），`quantity` 没有最小值限制。

**建议**:
```python
from enum import Enum
class OrderAction(str, Enum):
    BUY = "buy"
    SELL = "sell"

class PlaceOrderRequest(BaseModel):
    action: OrderAction = Field(..., description="操作类型")
    quantity: int = Field(..., ge=1, le=1000000, description="数量")
```

### L8. 缺少单元测试覆盖

**文件**: 整个项目  
**问题**: 项目中没有发现测试文件，所有核心模块（`engine.py`, `analyzers.py`, `indicators.py`, `data_loader.py` 等）缺少单元测试。

**建议**: 为核心模块添加 pytest 单元测试，特别关注：
- 指标计算正确性（`indicators.py`）
- 回测引擎结果准确性（`engine.py`）
- 数据加载缓存逻辑（`data_loader.py`）

---

## 五、修复优先级建议

| 优先级 | 问题编号 | 预计工时 |
|--------|----------|----------|
| P0（立即） | H1（沙箱逃逸风险） | 1h |
| P0（立即） | M4（异常信息泄露） | 0.5h |
| P1（本周） | H3（异常处理器） | 0.5h |
| P1（本周） | M1（代码重复） | 1h |
| P1（本周） | M5（线程泄漏） | 1h |
| P2（下周） | M2（参数优化并行） | 2h |
| P2（下周） | M3（缓存优化） | 1.5h |
| P2（下周） | L4（速率限制） | 1h |
| P3（后续） | M6-M8, L1-L3, L5-L8 | 视情况 |

---

## 六、架构评价

### 优点
1. **分层清晰**: API → Core → Data 三层架构，职责分明
2. **安全基线好**: 已有代码安全审计（`_validate_code_security`）、CORS 限制、异常信息脱敏
3. **缓存策略合理**: Parquet 本地缓存 + 内存缓存，减少网络请求
4. **降级设计完善**: AKShare 不可用时自动降级到模拟数据，保证可用性
5. **Pydantic 校验**: 所有 API 输入使用 Pydantic 模型校验

### 改进方向
1. 引入依赖注入框架（如 `dependency-injector`）替代全局变量
2. 添加 OpenAPI 文档（FastAPI 自带，但可补充更多描述）
3. 引入配置管理（如 `pydantic-settings`）替代硬编码常量
4. 添加结构化日志（JSON 格式），便于生产环境监控

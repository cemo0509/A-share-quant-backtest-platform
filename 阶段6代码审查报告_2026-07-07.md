# 阶段6 全面代码审查报告

**审查日期**: 2026-07-07
**审查范围**: 全项目（backend + frontend + electron）
**审查人**: AI Code Reviewer

---

## 一、项目概览

| 维度 | 数值 |
|------|------|
| 后端文件 | ~30 个 Python 文件 |
| 前端文件 | ~20 个 TSX/TS 文件 |
| Electron 文件 | 1 个 main.js |
| 预置策略 | 14 个 |
| API 路由 | 9 个路由模块 |
| 前端页面 | 11 个页面 + 5 个组件 |
| 代码总量 | 约 8000+ 行 |

---

## 二、🔴 高优先级问题

### 问题 1: `market.py` 趋势 API 存在 `get_index_data` 调用错误（重复 bug）

**文件**: `backend/api/market.py:62`
**严重性**: 🔴 高 — 运行时报错

```python:62:62:backend/api/market.py
index_data = detector.get_index_data(symbol)
```

`MarketStateDetector` 类没有 `get_index_data` 实例方法（这是模块级函数）。在 `stocks.py` 中已修复同样的问题，但 `market.py` 的 `/api/market/trend` 路由第 62 行使用了同样的错误调用。

**影响**: 调用 `/api/market/trend` 端点时抛出 `AttributeError`。

---

### 问题 2: 前端 `Optimize.tsx` 响应数据嵌套路径错误

**文件**: `frontend/src/pages/Optimize.tsx:98-99`
**严重性**: 🔴 高 — 数据无法正确显示

```typescript:97:101:frontend/src/pages/Optimize.tsx
const res = await runOptimize(req)
const data = res.data.data
setResults(data.data || [])
setBestParams(data.best_params || null)
```

`res.data.data` 已经是后端返回的 `data` 对象，但第 99 行又取了 `data.data`，这会导致结果始终为空。正确的应该是：
- `setResults(data.data || [])` → `setResults(data || [])`

**影响**: 优化结果页面永远显示空白表格。

---

### 问题 3: `Compare.tsx` 引用 `trade_count` 字段不存在

**文件**: `frontend/src/pages/Compare.tsx:132`
**严重性**: 🔴 高 — 数据显示错误

```typescript:131:133:frontend/src/pages/Compare.tsx
const metrics = record.data.metrics || {}
return metrics.trade_count || 0
```

后端 `BacktestMetrics` 的字段名是 `total_trades`，不是 `trade_count`。这会永远显示 0。

---

### 问题 4: `models/schemas.py` 中的模型未被实际 API 使用

**文件**: `backend/models/schemas.py`
**严重性**: 🟡 中 — 代码冗余

`schemas.py` 定义了完整的 Pydantic 请求/响应模型（`BacktestRequest`, `OptimizeRequest`, `OrderRequest`, `AccountInfo` 等），但所有 API 路由模块（`backtest.py`, `optimize.py`, `trading.py` 等）都在各自文件中重复定义了 `BaseModel` 子类，完全未使用 `schemas.py` 中的模型。

这导致：
- `schemas.py` 和实际 API 路由中的模型定义不一致（例如 `backtest.py` 中 `BacktestRequest` 的默认 `cash=1_000_000`，而 `schemas.py` 中 `initial_cash=100000.0`）
- 代码维护成本翻倍

---

## 三、🟡 中优先级问题

### 问题 5: `analyzer.py` `BacktestMetrics.to_dict()` 仍使用 `asdict()` 存在序列化风险

**文件**: `backend/core/analyzers.py:31-32`
**严重性**: 🟡 中

```python:31:32:backend/core/analyzers.py
def to_dict(self) -> dict:
    return asdict(self)
```

`engine.py` 中的 `BacktestResult.to_dict()` 已在上一次审查中修复为安全的 `_safe_convert`，但 `analyzers.py` 中的 `BacktestMetrics` 仍使用 `asdict()`。虽然 `BacktestMetrics` 的字段都是 Python 原生类型，风险较低，但为一致性应统一使用安全序列化。

---

### 问题 6: `stock_names.py` 硬编码映射包含不完整/错误的股票

**文件**: `backend/data/stock_names.py:73-96`
**严重性**: 🟡 中

```python:73:96:backend/data/stock_names.py
self._hardcoded = {
    '600103': ('青山纸业', '造纸'), ...
    '600741': ('华域汽车', '汽车零部件'), ...
}
```

硬编码列表仅包含 43 只股票，且部分代码可能不准确。该模块应该优先从 AKShare 获取全量数据。

---

### 问题 7: `indicators.py` KDJ 使用 `ewm` 近似而非标准递推公式

**文件**: `backend/core/indicators.py:34-43`
**严重性**: 🟡 中 — 指标计算准确性

```python:34:43:backend/core/indicators.py
def calc_kdj(high, low, close, n=9):
    low_n = low.rolling(n).min()
    high_n = high.rolling(n).max()
    rsv = ((close - low_n) / (high_n - low_n) * 100).round(2)
    k = rsv.ewm(com=2, adjust=False).mean().round(2)
    d = k.ewm(com=2, adjust=False).mean().round(2)
    j = (3 * k - 2 * d).round(2)
```

KDJ 的标准计算公式为递推式 `K[i] = 2/3 * K[i-1] + 1/3 * RSV[i]`，而 `ewm(com=2)` 的权重计算方式是 `alpha = 1/(1+com) = 1/3`，这与 `1/3` 的权重一致，但 `adjust=False` 模式下的初始值处理和标准 KDJ 有差异。`stocks.py` 中已有标准递推实现，这里应该保持一致。

---

### 问题 8: `stock_scan.py` 并发扫描缺乏进度报告

**文件**: `backend/api/stock_scan.py`
**严重性**: 🟡 中 — 用户体验

`_run_concurrent_scan` 使用 `ThreadPoolExecutor` 进行并发扫描，但扫描进度信息仅在日志中记录。前端通过轮询 `/api/scan-status/{task_id}` 获取状态，但扫描过程中的中间进度（已扫描 X/总数）未通过 API 暴露。

---

### 问题 9: `trading.py` 全局单例在并发环境下不安全

**文件**: `backend/api/trading.py:12`
**严重性**: 🟡 中 — 并发安全

```python:12:12:backend/api/trading.py
_trading_simulator = TradingSimulator()
```

全局 `TradingSimulator` 实例在多个并发请求下访问同一个账户状态（`available_cash`, `positions` 等），没有加锁保护。多个并发下单请求可能导致资金计算错误。

---

### 问题 10: `registry.py` `get_strategy()` 动态导入位置不一致

**文件**: `backend/core/strategies/registry.py:238-265`
**严重性**: 🟡 中 — 性能

```python:238:265:backend/core/strategies/registry.py
def get_strategy(key: str) -> StrategyInfo:
    if key in REGISTRY:
        return REGISTRY[key]
    try:
        from .custom_manager import load_custom_strategy_class  # 延迟导入
        strategy_cls = load_custom_strategy_class(key)
        from .custom_manager import list_custom_strategies  # 重复导入
        ...
```

`custom_manager` 已在文件顶部导入（第 23 行），但函数内部又重复 `from .custom_manager import ...`，虽然 Python 的模块缓存会避免重复加载，但这种写法不整洁。

---

### 问题 11: `StrategyEditor.tsx` 内联大量硬编码策略代码

**文件**: `frontend/src/pages/StrategyEditor.tsx:399-416`
**严重性**: 🟡 中 — 可维护性

`getPresetStrategyCode` 函数中硬编码了 14 个预置策略的完整 Python 源代码，代码量约 200 行。这导致：
- 前端文件过大（650 行）
- 策略代码修改需要同时改前端和后端
- 无法复用后端已有的策略代码

**建议**: 改为通过 API 从后端获取策略代码，或至少抽取为独立模块。

---

### 问题 12: `StockDetail.tsx` 依赖外部 iframe 存在可用性风险

**文件**: `frontend/src/pages/StockDetail.tsx:440-473`
**严重性**: 🟡 中 — 可用性

财务、公告、研报 Tab 使用东方财富 iframe 嵌入。如果东方财富页面变更或增加反爬措施，这些功能将失效。此外，iframe 无法正常工作在 Electron 的 `file://` 协议下（因 CSP 限制）。

---

## 四、🟢 低优先级问题

### 问题 13: `StrategyItem` 接口在 5 个文件中重复定义

| 文件 | 行数 |
|------|------|
| `pages/Backtest.tsx` | 11-17 |
| `pages/Optimize.tsx` | 12-17 |
| `pages/Compare.tsx` | 11-15 |
| `pages/StockScan.tsx` | 类似定义 |
| `pages/StrategyEditor.tsx` | 隐式使用 |

**建议**: 提取到 `stores/index.ts` 或 `api/index.ts` 中统一导出。

---

### 问题 14: `position_sizer.py` 和 `exit_rules.py` 未被任何 API 路由引用

**文件**: `backend/core/position_sizer.py`, `backend/core/exit_rules.py`
**严重性**: 🟢 低 — 死代码

这两个模块虽然功能完整且质量较高，但在 API 层没有任何路由使用它们。它们在策略模板中被引用，但通过前端编辑的自定义策略代码是独立运行的，不依赖这些模块。

---

### 问题 15: `engine.py` `run_backtest` 参数签名中的 `symbol` 直接用作文件查找

**文件**: `backend/core/engine.py`
**严重性**: 🟢 低 — 潜在问题

```python
bt_result = run_backtest(
    strategy_cls=strategy_cls,
    symbol=code,  # 去除了市场前缀
    ...
)
```

`run_backtest` 将 `symbol` 传给 `fetch_kline`，后者需要纯数字代码。但如果前端直接传带前缀的代码（如 `sh600519`），可能导致数据加载失败。应在 `engine.py` 层面做统一的前缀处理。

---

### 问题 16: 缺少前端全局错误边界

**文件**: `frontend/src/App.tsx`
**严重性**: 🟢 低 — 用户体验

React 应用没有设置 `ErrorBoundary` 组件。如果某个页面组件渲染出错，整个应用会白屏，用户体验不佳。

---

### 问题 17: `backend/main.py` 全局异常处理器暴露内部错误信息

**文件**: `backend/main.py:56`
**严重性**: 🟢 低 — 安全

```python:56:56:backend/main.py
content={"status": "error", "detail": f"服务器内部错误: {exc}"},
```

将异常信息直接返回给客户端可能泄露内部实现细节。生产环境应只返回通用错误信息。

---

### 问题 18: `frontend/src/api/index.ts` 导出函数名与 `services/api.ts` 重导出不一致

`api/index.ts` 中导出了 `exportResultJson` 和 `exportTradesCsv`，但 `services/api.ts` 中没有重新导出这两个函数。如果现有代码依赖 `services/api.ts` 路径导入这些函数，会报错。

---

## 五、总结

| 严重性 | 数量 | 关键问题 |
|--------|------|---------|
| 🔴 高 | 4 | market.py 调用错误、Optimize.tsx 数据路径错误、Compare.tsx 字段名错误、schemas.py 未使用 |
| 🟡 中 | 8 | analyzers 序列化、indicators KDJ 近似、并发安全、代码重复等 |
| 🟢 低 | 6 | 接口重复定义、死代码、错误边界等 |
| **合计** | **18** | |

### 与上次审查对比

- ✅ 上次修复的 10 个问题均已正确解决
- ⚠️ 新发现 18 个问题
- 🔄 问题 5（analyzer asdict）是上次遗漏的同类型问题
- 🔄 问题 1（market.py get_index_data）是上次修复的同类遗漏

# A股量化回测平台 — 阶段4 代码审查报告

> **审查日期**: 2026-07-07  
> **审查范围**: 全项目（后端 11 模块 + 前端 11 页面 + Electron 层）  
> **审查类型**: 全面代码质量审查  
> **项目版本**: v1.1.1

---

## 一、项目概况

| 维度 | 数据 |
|------|------|
| 后端文件数 | ~30 个 `.py` 文件（api/、core/、core/strategies/） |
| 前端文件数 | ~15 个 `.tsx`/`.ts` 文件 |
| 策略总数 | 14 个预置 + 自定义策略支持 |
| 后端核心模块 | engine, trading, data_loader, analyzers, indicators, market_state, position_sizer, exit_rules |
| 前端页面 | Dashboard, Backtest, StockScan, Optimize, Compare, StrategyEditor, DataManage, Results, Trading, RealtimeQuotes, StockDetail |
| 打包方案 | Electron + 嵌入式 Python + NSIS |

---

## 二、整体评分

| 评审维度 | 评分 (1-10) | 说明 |
|----------|:----------:|------|
| 代码结构 | 8 | 前后端分离清晰，模块职责明确 |
| 错误处理 | 7 | API层有统一的异常处理，部分模块需加强 |
| 性能 | 7 | Parquet缓存策略良好，批量扫描有并发优化 |
| 安全性 | 6 | CORS配置合理，但存在一些安全隐患 |
| 可维护性 | 7 | 模块化良好，但部分文件过长 |
| 测试覆盖 | 3 | 缺少自动化测试，仅有少量手动测试脚本 |
| 文档 | 6 | 代码注释较好，但缺少API文档和架构图 |

---

## 三、后端代码审查

### 3.1 `backend/main.py` — FastAPI 入口 ✅

**亮点**:
- CORS 中间件配置完善，同时支持开发、预览、Electron 三种环境
- 全局异常处理器避免了 traceback 泄露
- 健康检查端点 `/api/health` 供 Electron 启动轮询
- 打包环境自动关闭热重载

**问题**:
- **[中] L26**: `allow_origin_regex` 正则 `r"^(https?://localhost:\d+|null)$"` 在开发环境可能被绕过，建议打包环境下仅允许 `null`
- **[低] L64**: `is_packaged` 判断逻辑使用字符串匹配 `"resources"`，不够健壮

### 3.2 `backend/core/data_loader.py` — 数据加载层 ✅

**亮点**:
- 完善的 Parquet 缓存机制，一次下载永久使用
- 智能缓存目录选择（开发环境用项目目录，打包环境用 APPDATA）
- 60 秒内存缓存减少 AKShare API 请求
- 多级降级策略（AKShare → 模拟数据）
- 股票名称映射 4 级优先级加载（StockNameCache → 本地JSON → AKShare → 硬编码）
- 实时行情 50+ 字段完整覆盖，含涨跌统计计算

**问题**:
- **[中] L627**: `_calc_change_stats` 中 `total_shares = hist_df.get("total_share", pd.Series(...)).values` — K线缓存中通常不包含 total_share 列，会导致换手率始终为 0
- **[中] L634-637**: 3日涨幅计算使用 `closes[-4]` 而不是 `closes[-3]`，与预期语义不符（应该是 3 个交易日前，即 index 偏移 3）
- **[低] L719**: `_calc_consecutive_up` 的 `prev` 变量更新逻辑可能错误：`prev = c` 意味着第 2 天比较的是前一天的收盘价而非前一天的 `pre_close`，语义混乱
- **[低] L389**: 内存缓存 key 使用 `",".join(sorted(symbols))`，如果 `symbols` 顺序变化或长度不同，缓存会重复
- **[低] L827**: 文件总行数 827 行，建议拆分：股票名称映射可独立为 `stock_names.py`

### 3.3 `backend/core/engine.py` — 回测引擎 ✅

**亮点**:
- `run_backtest` 统一入口，支持策略 key 和类两种调用方式
- 参数过滤逻辑正确处理 Backtrader 的 AutoInfoClass
- 自定义 `_EquityCurveAnalyzer` 和 `_TradeRecordAnalyzer` 方便前端展示
- 完善的 Backtrader 分析器配置（SharpeRatio, DrawDown, TradeAnalyzer, AnnualReturn, TimeReturn）

**问题**:
- **[高] L18**: `print("DEBUG: engine.py module loaded")` — 生产环境应移除调试打印语句（多处）
- **[中] L78-87**: `PandasData` 初始化没有指定 `openinterest`，设为 -1 是正确做法但缺少注释说明
- **[中] L135**: `_TradeRecordAnalyzer` 同时记录 `notify_order` 和 `notify_trade`，会导致交易记录重复（一笔交易会同时触发 order 和 trade 事件）
- **[低] L101-122**: 参数过滤逻辑较复杂，建议抽为独立函数并添加单元测试

### 3.4 `backend/core/analyzers.py` — 指标分析 ✅

**亮点**:
- `BacktestMetrics` dataclass 结构清晰
- 支持从 Backtrader 分析器提取各项指标
- 年化收益率使用几何平均计算，符合金融工程规范

**问题**:
- **[中] L47**: `start_cash = cerebro.broker.startingcash` — 如果回测过程中 broker 的 cash 有变化，此处可能不准确
- **[低] L96-121**: `_extract_trades` 同时处理 dict 和 list 两种 `_trades` 格式，但实际 Backtrader 版本可能返回不同的结构，兼容性需测试

### 3.5 `backend/core/indicators.py` — 技术指标 ✅

**亮点**:
- 8 种常用技术指标（MA, MACD, KDJ, RSI, BOLL, WR, OBV, BIAS, CCI）
- 使用 pandas vectorized 操作，性能良好
- `calc_all_indicators` 统一计算接口

**问题**:
- **[低] L39-42**: KDJ 的 `ewm(com=2)` 与标准 KDJ 算法（2/3 权重）不完全一致，标准应使用 `ewm(alpha=1/3, adjust=False)`
- **[低] L97**: `rolling().apply()` 在数据量大时性能较差，建议用 numpy 实现

### 3.6 `backend/core/trading.py` — 模拟交易 ✅

**亮点**:
- 完整的数据模型（Order, Position, Account）
- TradingSimulator 封装了风控参数（止损5%、止盈10%、最大回撤20%）
- 滑点和佣金模拟

**问题**:
- **[高] L166**: `update_position` 先更新了持仓，后计算佣金，导致佣金从 `available_cash` 中扣减两次（第 101 行扣了交易金额，第 176 行又扣了佣金），实际可用资金计算错误
- **[中] L188-196**: `get_account_info` 中使用 `pos.current_price if pos.current_price > 0 else pos.cost_price` 来估算当前价，实际上 `current_price` 从未被更新（需要调用 `update_current_prices`），导致持仓市值不准确
- **[中] L12**: 全局单例 `_trading_simulator` 在并发请求时线程不安全
- **[低] L249-272**: `check_risk_management` 定义但从未被 `place_order` 调用

### 3.7 `backend/core/market_state.py` — 市场状态 ✅

**亮点**:
- 多维度市场状态判断（趋势、结构、波动率）
- 多周期趋势检测（5/10/20/60 日）

**问题**:
- **[中]**: `get_index_data` 函数使用 `__getitem__` 方式索引列，但 DataFrame 列的语义不明确（依赖 AKShare 返回的具体列名）

### 3.8 `backend/core/position_sizer.py` — 仓位管理 ✅

**亮点**:
- 凯利公式、目标波动率、ATR 三种仓位计算方法
- `PositionSizer` 类统一接口

### 3.9 `backend/core/exit_rules.py` — 退出规则 ✅

**亮点**:
- 5 种退出机制（追踪止损、时间退出、Dead Drift、硬止损、止盈、波动率止损）
- `SmartExitStrategy` 基类可直接被其他策略继承

### 3.10 `backend/core/strategies/` — 策略层 ✅

**亮点**:
- 14 个预置策略覆盖主流技术分析方法
- 策略注册表（registry.py）支持预置+自定义策略
- 自定义策略管理器支持动态加载
- 策略分类体系（trading/screening/hybrid）

**问题**:
- **[中]**: `mean_reversion.py` 和 `momentum.py` 直接继承 `bt.Strategy` 而非 `BaseStrategy`，与其他策略不一致，缺少统一的日志输出
- **[低]**: `grid_trading.py` 中 `center_type` 参数使用字符串判断，不如用枚举

### 3.11 `backend/api/stocks.py` — 股票 API ✅

**亮点**:
- K线数据支持多周期（日/周/月/5min/15min/30min/60min）
- 分时数据接口
- 技术指标一站式获取
- 策略信号计算（纯 numpy 实现，不依赖 Backtrader）

**问题**:
- **[中] L334-415**: `_ema`, `_sma`, `_rolling_std`, `_rsi`, `_kdj` 辅助函数中每次调用都 `import numpy as np`，应移到文件顶部
- **[中] L406-414**: KDJ 的 `k[i]` 在循环中先赋值 `k[i] = d[i] = 50`，然后立即被 `k[i] = 2/3 * k[i-1] + 1/3 * rsv` 覆盖，第一行的赋值无意义
- **[低] L85**: `/stocks/kline` 中的 symbol 格式需要 `sh/sz` 前缀，而 `/stocks/signals` 也需要同样前缀，但其他地方（如 `data_loader`）使用的是纯数字代码，格式不统一

### 3.12 `backend/api/backtest.py` — 回测 API ✅

**亮点**:
- 支持预置策略回测和自定义代码回测
- 策略比较接口
- 详细的错误日志

**问题**:
- **[低] L77-78**: `print("DEBUG: run_code called...")` 调试语句未清理

### 3.13 `backend/api/stock_scan.py` — 选股扫描 API ✅

**亮点**:
- 并发扫描（ThreadPoolExecutor, max_workers=8）
- SSE 实时进度推送 + 轮询降级方案
- 单只股票超时控制（10秒）
- 自选池持久化到 JSON

**问题**:
- **[中] L452-541**: `_run_single_scan` 中每个股票都启动 `run_backtest`，对于选股策略（只需要判断是否有信号），运行完整回测（含所有分析器）过于重量级，性能浪费
- **[中] L535**: `thread.join(timeout=timeout)` 在线程池环境中使用 threading 超时，可能导致线程泄漏（线程继续运行但结果被丢弃）
- **[低] L36**: `_SCAN_PROGRESS_LOCK` 使用 `__import__('threading').Lock()` 这种非标准写法，应改为顶部 `import threading`

### 3.14 `backend/api/optimize.py` — 参数优化 API ✅

**亮点**:
- 穷举搜索 + 多目标优化（夏普比率、总收益、回撤、年化、胜率）
- 导出功能（JSON/CSV）

**问题**:
- **[中] L80-121**: 穷举搜索是串行的，组合数多时性能极差（无并发优化），建议添加进度回调
- **[低] L98**: `max_drawdown` 作为优化目标时，`metric_value = -abs(m.get("max_drawdown", 100))` — 默认值 100 不合理

### 3.15 路由注册检查 ✅

| API 前缀 | 路由文件 | 状态 |
|----------|---------|:----:|
| `/api/backtest` | `backtest.py` | ✅ |
| `/api/data` | `data.py` | ✅ |
| `/api/strategy` | `strategy.py` | ✅ |
| `/api/trading` | `trading.py` | ✅ |
| `/api/stocks` | `stocks.py` | ✅ |
| `/api/optimize` | `optimize.py` | ✅ |
| `/api/export` | `export.py` | ✅ |
| `/api/market` | `market.py` | ✅ |
| `/api` | `stock_scan.py` | ✅ |

---

## 四、前端代码审查

### 4.1 `frontend/src/api/index.ts` — API 封装 ✅

**亮点**:
- 统一的 axios 实例配置（baseURL, timeout）
- TypeScript 类型定义（BacktestReq, StockScanReq 等）

**问题**:
- **[中] L4**: `baseURL: 'http://127.0.0.1:8000/api'` — 硬编码本地地址，打包环境下 Electron 加载 `file://` 协议时跨域无问题，但不利于部署到远程服务器
- **[低] L3**: 缺少请求/响应拦截器，无统一错误处理和 token 注入

### 4.2 `frontend/src/App.tsx` — 主布局 ✅

**亮点**:
- HashRouter 适配 Electron 打包环境
- 侧边栏导航 + 路由配置清晰

**问题**:
- **[低] L48-63**: 路由组件直接懒加载缺失（未使用 `React.lazy`），首次加载会包含所有页面代码

### 4.3 `frontend/src/components/RealtimeQuotes.tsx` — 实时行情 ✅

**亮点**:
- 30 秒自动刷新 + localStorage 持久化股票列表
- 完整的 QuoteData 接口定义（50+ 字段）
- 涨跌幅颜色区分（红涨绿跌）

**问题**:
- **[低] L4**: `import { getRealtimeQuotes } from '../services/api'` — 导入路径是 `../services/api` 而非 `../api`，与 `StockDetail.tsx` 中的 `../api` 不一致，需要确认 services/api 文件是否存在
- **[低] L110**: `setInterval(fetchQuotes, 30000)` 依赖 `symbols` 变化时重新创建，但旧定时器未被清理可能导致多个定时器同时运行

### 4.4 `frontend/src/pages/StockDetail.tsx` — 个股详情 ✅

**亮点**:
- 丰富的图表（K线 + 均线 + 买卖信号标记 + 成交量 + 技术指标）
- 多 Tab 设计（K线/分时/基本信息/财务/公告/研报）
- 信号策略切换（MACD/KDJ/RSI/均线交叉/布林带）
- 涨跌统计字段完整展示（带颜色标注）

**问题**:
- **[中] L46**: `getSignals` 仅在 `klinePeriod === 'daily'` 时调用，但周K/月K周期下 signalStrategy 切换不会触发任何数据更新
- **[低] L42-47**: 4 个 `Promise.all` 请求并行，若 `getSignals` 失败会整体抛异常（虽然加了 `.catch`），但错误提示不明确
- **[低] L114**: 买入点位置 `ohlc[idx][3] * 0.97`（最低点下方 3%），如果当天跌幅超过 3% 则标记点会出现在 K 线内部
- **[低] L475**: 文件 475 行，可考虑拆分图表逻辑到独立组件

### 4.5 `frontend/src/pages/Backtest.tsx` — 回测配置 ✅

**亮点**:
- 策略分类过滤（仅显示 trading/hybrid）
- 动态策略参数表单
- 错误处理完善（区分网络错误和业务错误）

### 4.6 `frontend/src/pages/StockScan.tsx` — 选股池 ✅

**亮点**:
- SSE 实时进度 + 轮询降级双重保障
- 组件卸载时自动清理 SSE 和定时器
- 批量加入自选池 + CSV 导出

**问题**:
- **[低] L13**: `const API_BASE = 'http://127.0.0.1:8000/api'` — 硬编码地址，与 axios 实例不一致
- **[低] L166-188**: `pollResults` 中的 `setInterval` 每秒轮询一次，可能给后端造成压力

### 4.7 `frontend/src/pages/Trading.tsx` — 交易管理 ✅

**亮点**:
- 账户信息、持仓、订单历史三合一展示
- 下单表单简洁实用

**问题**:
- **[低] L68-71**: 下单成功后调用 3 次数据刷新，可以合并为一次

### 4.8 `frontend/src/pages/StrategyEditor.tsx` — 策略编辑器 ✅

**亮点**:
- Monaco Editor 集成，支持 Python 语法高亮
- 5 个策略模板
- 编程模式 / 可视化模式切换
- 预置策略只读保护

**问题**:
- **[中] L4**: `import { getStrategies, ... } from '../services/api'` — 与 `api/index.ts` 路径不一致
- **[中] L620**: Monaco Editor 的 `readOnly` 判断逻辑复杂（`loading || (selectedKey !== '' && ...)`），建议抽为独立变量
- **[低] L398-417**: `getPresetStrategyCode` 函数中内联了大量策略代码字符串，建议移到独立文件或从后端获取

### 4.9 `frontend/src/pages/Optimize.tsx` — 参数优化 ✅

**亮点**:
- JSON 格式的参数网格输入
- 超过 100 组参数时确认提示
- 最优参数 Tag 展示

**问题**:
- **[低] L99**: `setResults(data.data || [])` — 这里 `res.data.data` 已经是 data 对象，再取 `.data` 可能是冗余的嵌套
- **[低] L97-100**: 优化请求是同步的（等待全部完成才返回），建议添加进度指示

### 4.10 `frontend/src/pages/Compare.tsx` — 策略比较 ✅

**亮点**:
- 多策略同时回测比较
- 结果表格展示关键指标

**问题**:
- **[低] L145**: "查看详细信息功能开发中..." — 功能未实现

---

## 五、Electron 层审查

### 5.1 `electron/main.js` — 主进程 ✅

**亮点**:
- Python 解释器查找策略完善（内置 Python → py launcher → python → python3）
- 后端端口占用清理
- 后端健康检查轮询（30 次 × 1 秒）
- 多路径前端文件查找（适配不同打包环境）
- 窗口关闭时自动停止后端

**问题**:
- **[中] L74-93**: `killPortProcess` 仅支持 Windows，Linux/macOS 上打包后端口冲突无法自动解决
- **[中] L212**: Windows 上 `spawn('taskkill', ['/pid', ...])` 可能无法正确杀掉 Python 子进程树（如果子进程又启动了子进程）
- **[低] L18**: 打包环境路径 `path.join(resourcesPath, 'python', 'python.exe')` — 需要确保 `electron-builder.yml` 中 `extraResources` 配置与此路径一致
- **[低] L342-354**: `createWindow` 不等待后端就绪就加载前端，前端首页会显示"后端离线"，用户体验可优化

### 5.2 `electron/preload.js` ✅

13 行的简洁预加载脚本，通过 contextBridge 暴露安全的 API，设计合理。

### 5.3 `electron-builder.yml` ✅

配置完整，支持 Windows (NSIS)、macOS (DMG)、Linux (AppImage) 三平台打包。

---

## 六、严重问题汇总（需优先修复）

| 序号 | 严重程度 | 文件 | 问题描述 |
|:----:|:--------:|------|----------|
| 1 | 🔴 高 | `core/engine.py` L18, L62-64 | 生产环境残留 DEBUG print 语句，应在打包时移除 |
| 2 | 🔴 高 | `core/trading.py` L166 | 佣金被重复扣减：`update_position` 扣了一次，`place_order` 又扣一次 |
| 3 | 🔴 高 | `core/trading.py` L188-196 | `current_price` 从未被更新，持仓市值计算不准确 |
| 4 | 🟡 中 | `core/data_loader.py` L627 | `total_share` 列在 K 线缓存中通常不存在，换手率始终为 0 |
| 5 | 🟡 中 | `core/data_loader.py` L634 | 3 日涨幅计算偏移量错误（应使用 `closes[-3]` 而非 `closes[-4]`） |
| 6 | 🟡 中 | `api/stock_scan.py` L452-541 | 选股扫描对每只股票运行完整回测（含所有分析器），性能浪费 |
| 7 | 🟡 中 | `api/stocks.py` L334-415 | 辅助函数内重复 `import numpy as np` |
| 8 | 🟡 中 | `api/stock_scan.py` L535 | ThreadPoolExecutor 内使用 `thread.join(timeout)` 可能导致线程泄漏 |

---

## 七、改进建议

### 7.1 架构层面

1. **添加服务层**: 当前 API 层直接调用 core 模块，建议添加 service 层处理业务逻辑
2. **配置管理**: 硬编码值（如 BACKEND_PORT=8000、缓存 60 秒等）应提取到配置文件
3. **异步优化**: 回测和优化接口是同步阻塞的，建议使用 `BackgroundTasks` 或 `asyncio.to_thread`
4. **API 文档**: 建议集成 FastAPI 自带的 OpenAPI/Swagger 文档

### 7.2 代码质量

1. **统一 import 路径**: 前端存在 `../api` 和 `../services/api` 两种导入方式，需统一
2. **文件拆分**: `data_loader.py` (827行)、`StrategyEditor.tsx` (650行) 建议拆分
3. **移除调试代码**: 所有 `print("DEBUG: ...")` 语句应在生产环境移除
4. **添加类型标注**: 部分函数缺少返回类型标注

### 7.3 测试

1. **添加单元测试**: 核心计算函数（indicators、analyzers、data_loader）应有单元测试
2. **添加集成测试**: API 端点的集成测试
3. **添加前端测试**: 关键组件的渲染测试

### 7.4 安全

1. **自定义代码执行**: `load_strategy_from_code` 允许执行任意 Python 代码，需要沙箱隔离
2. **输入验证**: 部分 API 参数缺少长度和格式验证

---

## 八、总结

项目整体代码质量**良好**，架构设计合理，前后端职责清晰。v3.0 升级后功能完整度较高（50+ 实时行情字段、技术指标、策略信号、选股扫描等）。

主要风险点集中在：
1. **模拟交易模块**的佣金计算和持仓市值存在 bug
2. **选股扫描**的性能可以进一步优化
3. **自定义策略执行**缺少安全沙箱

建议在下一阶段优先修复上述 8 个严重问题，然后补充测试覆盖率和 API 文档。

---

> **审查人**: AI Code Reviewer  
> **下次审查建议时间**: 修复上述问题后进行回归审查

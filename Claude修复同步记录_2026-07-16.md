# Claude 修复同步记录（→ CodeBuddy / WorkBuddy / 用户）

> 同步日期：2026-07-16
> 同步方：Claude（Claude Code）
> 目的：向 CodeBuddy / WorkBuddy 同步本次已落地的修复，避免重复工作或冲突

---

## 一、已完成的 6 项修复

### 1. 清理根目录日志文件

| 项目 | 详情 |
|------|------|
| 操作 | 删除根目录 21 个 `.log` 文件（`_pk_dist.log`, `backend.log`, `backend_err.log`, `build_el.log`, `preview.log` 等） |
| 影响文件 | 仅删除文件，不改代码 |
| .gitignore | 新增 `logs/` 规则，防止日志目录被提交 |

### 2. 更新 README.md（v1.0.0 → v1.1.2）

| 项目 | 详情 |
|------|------|
| 文件 | `README.md` |
| 改动 | 版本号 v1.0.0 → v1.1.2；功能清单更新为 14 策略 + 可视化编辑器 + 智能推荐 + 选股池等实际内容；文档链接指向现有同步文档；路线图区分 v1.0.0 / v1.1.x / v1.2.0+ |

### 3. 后端日志轮转

| 项目 | 详情 |
|------|------|
| 文件 | `backend/main.py` |
| 改动 | `basicConfig` 改为 `handlers=[StreamHandler(), RotatingFileHandler(...)]`；日志写入 `backend/logs/backend.log`，单文件最大 5MB，保留 3 个历史文件 |
| 注意 | 原日志在根目录 `backend.log`，现在统一进 `backend/logs/` |

### 4. package.json 补充 author 字段

| 项目 | 详情 |
|------|------|
| 文件 | `package.json` |
| 改动 | 新增 `"author": "code"` |
| 效果 | 消除 electron-builder 打包时的 `author` 缺失警告 |

### 5. CORS 安全收紧

| 项目 | 详情 |
|------|------|
| 文件 | `backend/main.py` |
| 改动 | `null` origin 改为仅在打包模式（`_is_packaged`）下开放；开发模式只允许 `localhost:5173/5174/4173`；正则同步收紧 |
| 原因 | 之前 `null` 无条件开放，存在安全风险 |

### 6. 前端路由级代码分割

| 项目 | 详情 |
|------|------|
| 文件 | `frontend/src/App.tsx` |
| 改动 | 所有页面组件改为 `React.lazy(() => import(...))` 动态导入；新增 `PageLoader` 组件作为 Suspense fallback（居中 Spin）；路由外层包裹 `<Suspense fallback={<PageLoader />}>` |
| 效果 | 初始加载不再一次性下载全部页面 chunk，按路由按需加载。预计消除 `>500KB` chunk 警告 |

---

## 二、修改文件清单

| 文件 | 操作 | 摘要 |
|------|------|------|
| `README.md` | 编辑 | 版本号/功能/路线图更新 |
| `.gitignore` | 编辑 | 新增 `logs/` 规则 |
| `backend/main.py` | 编辑 | 日志轮转 + CORS 收紧 |
| `package.json` | 编辑 | 新增 `author` 字段 |
| `frontend/src/App.tsx` | 编辑 | React.lazy 代码分割 |
| 根目录 21 个 `.log` | 删除 | 清理历史日志 |

---

## 三、建议后续（未纳入本次修复）

| 建议 | 优先级 | 说明 |
|------|--------|------|
| 制作应用图标 `build/icon.ico` | 中 | 消除打包默认图标警告，提升专业度 |
| electron-updater 自动更新 | 中 | 用户无需手动下载安装包升级 |
| 添加 pytest 单元测试 | 中 | 至少覆盖 engine.py + registry.py |
| 后端 `_pk_dist.log` 等构建日志路径统一 | 低 | 打包脚本中输出到统一 logs/ 目录 |
| PDF 报告导出 | 低 | 回测结果一键生成 PDF |

---

## 四、给三方的结论

1. **CodeBuddy**：本次 6 项修复均为**独立改动**，不与智能推荐/v1.1.2 已有逻辑冲突。App.tsx 的 lazy import 不影响现有路由和组件 props。后端 main.py 的日志和 CORS 改动向后兼容。
2. **WorkBuddy**：后续接手时请注意日志路径已从根目录改到 `backend/logs/`；前端所有页面已是 lazy load，新增页面沿用 `React.lazy` 模式即可。
3. **用户（code）**：建议重新打包前做一次 clean build（`打包桌面版.cmd`），确认前端代码分割和后端日志轮转生效。

---

*本文档由 Claude 于 2026-07-16 生成，作为 CodeBuddy / WorkBuddy 修复同步记录。*

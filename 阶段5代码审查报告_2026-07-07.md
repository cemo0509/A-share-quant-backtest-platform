# 阶段5 代码审查报告

**审查日期**: 2026-07-07  
**审查范围**: 全项目（Backend 34文件 + Frontend 20文件 + Electron 2文件）  
**审查版本**: v1.1.1  

---

## 一、审查总结

| 维度 | 评级 | 说明 |
|------|------|------|
| 代码质量 | ⭐⭐⭐⭐ | 整体良好，历史问题已修复 |
| 架构设计 | ⭐⭐⭐⭐ | 分层清晰，模块化良好 |
| 错误处理 | ⭐⭐⭐⭐ | 异常处理较完善 |
| 安全性 | ⭐⭐⭐ | 有改进空间 |
| 性能 | ⭐⭐⭐ | 参数优化和选股扫描有优化空间 |
| 可维护性 | ⭐⭐⭐ | 存在重复代码和硬编码 |

**本次发现**: 12个问题（3个高优、6个中优、3个低优）

---

## 二、高优先级问题

### 🔴 问题1: `stock_scan.py` 中 `_run_single_scan` 的 `timeout` 参数无效

**文件**: `backend/api/stock_scan.py:452`  
**严重程度**: 🔴 高  

```python
def _run_single_scan(
    symbol, strategy_cls, params, start_date, end_date, timeout=10
) -> Optional[dict]:
```

`timeout` 参数被传入函数，但函数内部调用 `run_backtest()` 是同步阻塞调用，没有实际使用 `timeout` 参数来控制超时。真正的超时控制依赖于外层的 `future.result(timeout=5)`，这是两回事。建议移除函数签名中无效的 `timeout` 参数，避免误导。

---

### 🔴 问题2: `optimize.py` 穷举搜索无进度反馈和超时保护

**文件**: `backend/api/optimize.py:65-141`  
**严重程度**: 🔴 高  

`optimize_params` 对所有参数组合做穷举扫描，没有任何进度反馈机制。当参数组合数量较大时（如 10x10x10=1000组），API 请求会长时间阻塞（HTTP 超时120秒可能不够），前端无法获取进度。建议：
- 添加异步扫描进度机制（类似选股扫描的 SSE 方案）
- 或至少增加中间进度日志
- 前端需要更大的超时时间

---

### 🔴 问题3: `stock_scan.py` 中存在硬编码的兜底股票列表

**文件**: `backend/api/stock_scan.py:399-404`  
**严重程度**: 🔴 高  

```python
if not all_symbols:
    all_symbols = [
        "sh600000", "sh600036", "sh600519", ...
    ]
```

当缓存为空时，使用硬编码的20只股票作为扫描范围。这会导致选股扫描结果严重不完整（全市场5000+只股票，兜底只有20只）。建议：
- 在兜底列表为空时返回错误提示，而不是悄悄使用不完整的列表
- 或者明确告知用户当前缓存状态

---

## 三、中优先级问题

### 🟡 问题4: 前端 API 模块重复

**文件**: `frontend/src/api/index.ts` 和 `frontend/src/services/api.ts`  
**严重程度**: 🟡 中  

存在两个功能高度重叠的 API 封装模块：
- `api/index.ts` - 被 `Backtest.tsx`、`Trading.tsx`、`StockScan.tsx` 等使用
- `services/api.ts` - 被 `StrategyEditor.tsx`、`Optimize.tsx`、`RealtimeQuotes.tsx` 等使用

两者创建了不同的 axios 实例，部分接口定义重复（如 `getStrategies`、`runBacktest` 在两处都有），造成维护困难。建议统一为一个 API 模块。

---

### 🟡 问题5: `market_state.py` 中 `get_index_data` 函数的 `MarketStateDetector` 类中不存在

**文件**: `backend/api/stocks.py:423`  
**严重程度**: 🟡 中  

```python
index_data = _market_detector.get_index_data(symbol)
```

`MarketStateDetector` 类中没有定义 `get_index_data` 方法。该方法实际是一个模块级别的函数 `get_index_data`（定义在 `market_state.py:209`），但代码却把它作为实例方法调用。这会导致 `AttributeError`。

---

### 🟡 问题6: `stocks.py` 中 KDJ 策略的 K 线计算逻辑错误

**文件**: `backend/api/stocks.py:397-408`  
**严重程度**: 🟡 中  

在 `_kdj()` 函数中，KDJ 计算的迭代循环有问题：
```python
for i in range(n - 1, len(closes)):
    # ... 计算 rsv
    if i == n - 1:
        k[i] = d[i] = 50  # 初始值
    k[i] = 2/3 * k[i - 1] + 1/3 * rsv   # 这里 i == n-1 时 k[i-1] 是 0
    d[i] = 2/3 * d[i - 1] + 1/3 * k[i]
    j[i] = 3 * k[i] - 2 * d[i]
```

当 `i == n-1` 时，先设置 `k[i] = d[i] = 50`，但紧接着 `k[i] = 2/3 * k[i-1] + 1/3 * rsv` 会覆盖掉刚设置的值。初始值 50 应该用于第一个有效位置 `n-1`，但当前逻辑是先设50再覆盖。正确做法应该是将初始值50的处理放在循环外部或使用条件分支。

---

### 🟡 问题7: `data_loader.py` 中 `_STOCK_NAME_MAP_LOCK` 初始化方式不规范

**文件**: `backend/core/data_loader.py:53`  
**严重程度**: 🟡 中  

```python
_STOCK_NAME_MAP_LOCK = __import__('threading').Lock()
```

虽然从上一轮审查中已改为使用 `__import__`，但这仍然不是标准的 Python 写法。标准做法应该是：
```python
import threading
_STOCK_NAME_MAP_LOCK = threading.Lock()
```

`__import__` 是底层 API，通常不应在正常代码中使用。

---

### 🟡 问题8: `custom_manager.py` 中动态加载策略模块可能造成内存泄漏

**文件**: `backend/core/strategies/custom_manager.py:128-156`  
**严重程度**: 🟡 中  

`load_custom_strategy_class()` 函数每次调用都会创建新的模块并注册到 `sys.modules`，但永不清理。如果多次加载同一策略（虽然模块名相同会覆盖），或是反复调用 `load_strategy_from_code()`（虽然已有 `pop` 处理），长时间运行后 `sys.modules` 会膨胀。

---

### 🟡 问题9: `engine.py` 中 `BacktestResult.to_dict()` 使用 `asdict()` 可能包含不可序列化对象

**文件**: `backend/core/engine.py:31-32`  
**严重程度**: 🟡 中  

```python
def to_dict(self) -> dict:
    return asdict(self)
```

`BacktestResult` 的 `metrics` 字段是 `BacktestMetrics` 对象，而 `asdict()` 会递归转换。但若未来 `metrics` 包含 numpy 类型或其它不可序列化对象，直接返回给 FastAPI 可能导致 JSON 序列化失败。目前 `run_backtest()` 已手动调用 `metrics.to_dict()` 做了处理，但 `BacktestResult.to_dict()` 直接使用 `asdict()` 仍是一个隐患。

---

## 四、低优先级问题

### 🟢 问题10: `StrategyEditor.tsx` 中存在大量硬编码的策略代码模板

**文件**: `frontend/src/pages/StrategyEditor.tsx:398-416`  
**严重程度**: 🟢 低  

`getPresetStrategyCode()` 函数中硬编码了10+个策略的完整 Python 代码。当后端策略逻辑更新时，前端展示的代码可能不一致。建议将策略模板移到独立文件或通过 API 从后端获取。

---

### 🟢 问题11: `StockScan.tsx` 中 `API_BASE` 变量重复定义

**文件**: `frontend/src/pages/StockScan.tsx:13`  
**严重程度**: 🟢 低  

```typescript
const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://127.0.0.1:8000/api'
```

这个变量在 `api/index.ts` 和 `services/api.ts` 中已经定义。StockScan 中的 `fetch()` 调用绕过了 axios 封装直接使用 `fetch` API，不一致且丢失了 axios 的错误处理逻辑。建议统一使用 `api/index.ts` 中的封装方法。

---

### 🟢 问题12: 多个文件中存在 `import` 语句在函数体内

**文件**: 多个文件（`data_loader.py`、`stock_scan.py`、`stocks.py` 等）  
**严重程度**: 🟢 低  

在函数体内部使用 `import` 语句，如：
- `data_loader.py:334` - `import akshare as ak` 在 `_ensure_stock_name_map()` 中
- `stock_scan.py:365` - `import akshare as ak` 在 `_get_scan_symbols()` 中
- `stocks.py:81` - `import akshare as ak` 在 `get_kline_data()` 中

这些 lazy import 虽然有其合理性（避免模块导入失败影响整体），但分散在各处不利于依赖管理。建议统一处理：在模块顶部导入，用 try/except 包裹并设置标志位。

---

## 五、架构建议

### 1. 统一 API 模块
建议将 `frontend/src/api/index.ts` 和 `frontend/src/services/api.ts` 合并为一个统一的 API 模块，消除重复定义。

### 2. 后端配置集中化
建议将 `BACKEND_PORT`、缓存目录、数据目录等配置集中到 `backend/config.py` 文件中，而不是分散在多个文件中各自硬编码。

### 3. 异步扫描/优化进度
建议将参数优化（`optimize.py`）也改为异步+SSE进度推送模式，与选股扫描（`stock_scan.py`）保持一致。

### 4. 增加日志级别控制
目前多处使用 `logger.debug()`，但 `main.py` 中 `logging.basicConfig(level=logging.INFO)` 会忽略所有 DEBUG 日志。建议通过环境变量控制日志级别。

---

## 六、测试覆盖建议

当前项目有少量测试脚本（`test_backend.py`、`test_phase2.py` 等），但缺少：
- 单元测试（pytest）
- API 集成测试
- 前端组件测试

建议后续逐步增加测试覆盖。

---

## 七、各文件评审摘要

| 文件 | 状态 | 备注 |
|------|------|------|
| `backend/main.py` | ✅ 良好 | CORS 配置、异常处理完善 |
| `backend/core/data_loader.py` | ⚠️ 有改进 | 问题7 |
| `backend/core/engine.py` | ⚠️ 有改进 | 问题9 |
| `backend/core/analyzers.py` | ✅ 良好 | - |
| `backend/core/trading.py` | ✅ 良好 | - |
| `backend/core/exit_rules.py` | ✅ 良好 | - |
| `backend/core/market_state.py` | ✅ 良好 | - |
| `backend/core/position_sizer.py` | ✅ 良好 | - |
| `backend/core/indicators.py` | ✅ 良好 | - |
| `backend/core/strategies/base.py` | ✅ 良好 | - |
| `backend/core/strategies/registry.py` | ✅ 良好 | - |
| `backend/core/strategies/custom_manager.py` | ⚠️ 有改进 | 问题8 |
| `backend/core/strategies/adaptive_strategy.py` | ✅ 良好 | - |
| `backend/api/backtest.py` | ✅ 良好 | - |
| `backend/api/stock_scan.py` | 🔴 有问题 | 问题1、3 |
| `backend/api/stocks.py` | 🔴 有问题 | 问题5、6 |
| `backend/api/trading.py` | ✅ 良好 | - |
| `backend/api/optimize.py` | 🔴 有问题 | 问题2 |
| `backend/data/stock_names.py` | ✅ 良好 | - |
| `backend/models/schemas.py` | ✅ 良好 | 部分 schema 未被使用 |
| `electron/main.js` | ✅ 良好 | - |
| `electron/preload.js` | ✅ 良好 | - |
| `frontend/src/App.tsx` | ✅ 良好 | - |
| `frontend/src/api/index.ts` | ⚠️ 有改进 | 问题4 |
| `frontend/src/services/api.ts` | ⚠️ 有改进 | 问题4 |
| `frontend/src/stores/index.ts` | ✅ 良好 | - |
| `frontend/src/pages/Backtest.tsx` | ✅ 良好 | - |
| `frontend/src/pages/Trading.tsx` | ✅ 良好 | - |
| `frontend/src/pages/StockScan.tsx` | ⚠️ 有改进 | 问题11 |
| `frontend/src/pages/StrategyEditor.tsx` | ⚠️ 有改进 | 问题10 |
| `frontend/src/pages/Optimize.tsx` | ✅ 良好 | - |
| `frontend/src/pages/Dashboard.tsx` | ✅ 良好 | - |
| `frontend/src/components/RealtimeQuotes.tsx` | ✅ 良好 | - |
| `frontend/src/components/KLineChart.tsx` | ✅ 良好 | - |

---

**审查人**: AI Code Reviewer  
**下一步**: 建议优先修复3个高优问题，特别是问题5（会导致运行时崩溃）和问题6（指标计算错误）

# 功能清单（当前已实现 · 桌面端 v1.1.2）

> 生成日期：2026-07-15
> 生成方：CodeBuddy
> 目的：直接盘点 v1.1.2 已具备的全部功能，纠正《项目未完成事项梳理_截至v1.1.2.md》中 B 类"状态不明"的误判（那 8 条实际已全部实现）。
> 验证方式：直接检索 `backend/`（Python）与 `frontend/src/`（TS/React）源码，非凭记忆。
> 安装包：`dist_build\A股量化回测平台 Setup 1.1.2.exe`

---

## 一、平台基础（已确认完成）

| 模块 | 内容 | 证据 |
|------|------|------|
| 后端引擎 | FastAPI + Uvicorn + Backtrader 回测 | `backend/main.py` |
| 后端 API | 多组 API（backtest/data/strategy/trading/stocks/optimize/export/market/stock_scan/monitor/visual_editor 等） | `backend/api/*` |
| 前端框架 | React + TS + Vite + Ant Design（darkAlgorithm 深色） | `frontend/src/*` |
| 桌面壳 | Electron 33 + electron-builder 24，NSIS 安装包 | `electron/` + `electron-builder.yml` |
| 嵌入式 Python | 打包进 exe 的 `python-embed/`，用户无需装 Python | `python-embed/` |

---

## 二、可视化策略编辑器（核心，已确认完成）

| 功能 | 说明 | 证据 |
|------|------|------|
| 404 修复 | 选中非 visual 策略强制切 code 模式 | 同步说明确第四节 |
| 配色修复 | 全部改用 `theme.useToken()` 语义色 | 同步说明确第四节 |
| between 校验 | `findBetweenMissing` 递归校验 | 同步说明确第四节 |
| 智能推荐模式 | 4 预设（macd金叉/rsi<30/kdj金叉/均线多头）+ 自动预填 + 两段式指标库 + preset_key + recommended_indicators | `backend/api/visual_editor.py` + `frontend/src/components/visual-editor/*` |
| 参数优化接口 | 多进程 pickle 修复，9 组参数按夏普排序 | 历史对话 |

---

## 三、早期需求功能（经源码核实，全部已实现）

| # | 需求 | 状态 | 后端证据 | 前端证据 |
|---|------|------|----------|----------|
| 1 | 选股池（保存/管理股票池，供策略调用） | ✅ 已做 | `backend/api/stock_scan.py`、`backend/core/screen_factors.py` | `frontend/src/pages/StockScan.tsx`、`App.tsx` 路由「选股池」 |
| 2 | 实时行情（盘口/委比/涨跌幅等） | ✅ 已做 | `backend/api/data.py:get_realtime_quotes`、`backend/core/realtime_monitor.py`、`backend/core/market_state.py` | `frontend/src/api/index.ts:getRealtimeQuotes` |
| 3 | 策略类型区分（screening vs trading） | ✅ 已做 | `backend/core/strategies/registry.py:33` 定义 `category: str = "trading" # screening/trading/hybrid`，注册表含 screening 类 | — |
| 4 | 个股详情页（K线/分时 + 技术指标叠加） | ✅ 已做 | `backend/api/`（K线/分时/指标接口） | `frontend/src/pages/StockDetail.tsx`（MACD/KDJ/RSI/BOLL/CCI/WR/OBV/BIAS） |
| 5 | CCI+MACD 双因子选股策略 | ✅ 已做 | `backend/core/strategies/cci_macd_selection.py` | — |
| 6 | 交互式条件选股（东财+同花顺融合界面） | ✅ 已做 | `backend/api/stock_scan.py`、`backend/core/screen_factors.py` | `frontend/src/pages/StockScan.tsx` |
| 7 | 东财式 K线/分时 + 财务/公告/研报 iframe | ✅ 已做 | — | `StockDetail.tsx:531-563` 财务/公告/研报东方财富 iframe（带加载失败回退）；K线/分时自绘已实现 |
| 8 | 盘中实时监控引擎（交易时段定时扫描） | ✅ 已做 | `backend/core/realtime_monitor.py`、`backend/api/monitor.py` | `frontend/src/pages/RealtimePool.tsx`「实时选股池」 |

---

## 四、明确未做 / 待做（真实状态）

| 项 | 优先级 | 说明 |
|----|--------|------|
| dual_ma / bollinger 智能推荐（两列比较） | 中 | 需扩展 `ConditionLeaf` 加 `targetLine`/`targetParams`，本期故意未做 |
| 前端 chunk > 500KB 警告 | 低 | 不影响功能，可 code-splitting |
| `package.json` 缺 `author` 字段 | 低 | 打包警告，纯元数据（用户曾尝试添加后又撤销，当前无） |
| 默认 Electron 图标 | 低 | 打包警告，需放 `build/icon.ico` 并在 `electron-builder.yml` 指定（当前未配置） |
| `ruleName` 时序偶发滞后 | 观察 | 实测无碍 |

---

## 五、已决策不做

| 项 | 结论 |
|----|------|
| 服务器/云端部署 | 单用户本地使用，当前不需要 |
| 重组股弹簧策略集成（阶段性） | 早期"先不做"，用户曾表态"都想要"但未正式启动 → 待用户决策后写新需求文档 |

---

## 六、给三方的结论

1. **用户（code）**：早期提的多项功能需求（选股池/实时行情/个股详情/CCI选股/条件选股/东财iframe/实时监控）**实际已全部落地 v1.1.2**，无需再追问"做没做"。
2. **WorkBuddy**：接手/并行开发时，上述功能已存在，勿重复实现；如需扩展请基于现有 `backend/api/stock_scan.py`、`monitor.py`、`frontend/src/pages/StockScan.tsx`、`StockDetail.tsx`、`RealtimePool.tsx` 改动。
3. **CodeBuddy**：本清单为功能盘点基线；后续新需求以代码为准，勿仅凭同步文档判断"未做"。

*本文档由 CodeBuddy 于 2026-07-15 查代码生成，作为三方功能对齐基线。*

# 手动打包步骤

## 问题分析
electron二进制文件下载失败，导致打包卡住。需要手动下载electron并配置。

## 打包步骤

### 步骤1：以管理员身份打开命令提示符（CMD）

右键点击"开始"菜单 -> "命令提示符(管理员)" 或 "Windows PowerShell(管理员)"

### 步骤2：执行以下命令

```cmd
cd /d C:\Users\22864\CodeBuddy\量化软件开发
```

### 步骤3：设置npm镜像（加速下载）

```cmd
npm config set registry https://registry.npmmirror.com
npm config set electron_mirror https://cdn.npmmirror.com/binaries/electron/
npm config set electron_builder_binaries_mirror https://cdn.npmmirror.com/binaries/electron-builder-binaries/
```

### 步骤4：重新安装electron

```cmd
cd node_modules\electron
node install.js
cd ..\..
```

### 步骤5：验证electron是否安装成功

```cmd
dir node_modules\electron\dist\electron.exe
```

如果这个文件存在，说明electron安装成功了。

### 步骤6：如果步骤5失败，手动下载electron

1. 访问：https://registry.npmmirror.com/-/binary/electron/33.4.11/electron-v33.4.11-win32-x64.zip
2. 下载这个zip文件
3. 解压到 `C:\Users\22864\CodeBuddy\量化软件开发\node_modules\electron\dist\` 目录
4. 确保 `electron.exe` 在 dist 目录中

### 步骤7：构建前端（如果还没构建）

```cmd
cd frontend
call "C:\Program Files\nodejs\npm.cmd" run build
cd ..
```

### 步骤8：复制前端到electron目录

```cmd
if not exist electron\dist mkdir electron\dist
xcopy /y /e frontend\dist\* electron\dist\
```

### 步骤9：执行打包

```cmd
call "C:\Program Files\nodejs\npm.cmd" run dist
```

## 如果还是失败，尝试打包为目录模式（不创建安装程序）

```cmd
call "C:\Program Files\nodejs\npm.cmd" run pack
```

这会在 `dist\win-unpacked` 目录生成可执行文件，你可以直接运行 `A股量化回测平台.exe`

## 清理命令（如果遇到奇怪错误）

```cmd
cd C:\Users\22864\CodeBuddy\量化软件开发
rd /s /q node_modules
call "C:\Program Files\nodejs\npm.cmd" install
```

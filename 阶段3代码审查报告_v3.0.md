# 阶段3 代码审查报告 — v3.0 升级

> 审查日期：2026-07-06
> 审查范围：v3.0 升级全部 14 个文件

---

## 一、审查摘要

| 维度 | 评级 | 说明 |
|------|------|------|
| 功能完整性 | ✅ 良好 | 5 个需求全部实现 |
| 代码质量 | ⚠️ 中等 | 存在 4 个需要修复的问题 |
| 类型安全 | ✅ 良好 | TypeScript 编译通过 |
| 异常处理 | ⚠️ 中等 | 部分路径缺失处理 |
| 性能 | ⚠️ 中等 | 选股扫描存在潜在性能瓶颈 |

---

## 二、发现的 Bug（🔴 必须修复）

### Bug #1 — `stock_scan.py` 调用 `run_backtest` 参数名不匹配

**文件**: `backend/api/stock_scan.py:270-279`
**严重等级**: 🔴 **运行时崩溃**

```python
# 当前代码 (stock_scan.py L270-279)
bt_result = run_backtest(
    strategy_cls=strategy_cls,     # ✅ 正确
    symbol=code,                    # ✅ 正确
    start_date=start_date,         # ✅ 正确
    end_date=end_date,             # ✅ 正确
    params=params,                 # ✅ 正确
    cash=1000000,                  # ✅ 正确
    commission=0.0003,            # ✅ 正确
    period="daily",               # ✅ 正确
)
```

**分析**: 实际上调用参数与 `engine.py` 的 `run_backtest()` 函数签名完全匹配。✅ **不存在 Bug**。

但是我发现了另一个问题：

**检查 `FactorScoreStrategy` 的基类**：

```python
# registry.py L228
"factor_score": StrategyInfo(
    ...
    strategy_cls=FactorScoreStrategy,  # 继承 bt.Strategy，而非 BaseStrategy
    ...
)
```

而其他策略如 `MABullishStrategy` 继承 `BaseStrategy`：
```python
# ma_bullish.py L14
class MABullishStrategy(BaseStrategy):  # 继承 BaseStrategy
```

但 `FactorScoreStrategy` 继承 `bt.Strategy`：
```python
# factor_strategy.py L14
class FactorScoreStrategy(bt.Strategy):
```

**影响**: 如果 `BaseStrategy` 中有共享逻辑（如持仓管理、日志），`FactorScoreStrategy` 将缺失这些功能。需要确认是否所有策略都应在 `BaseStrategy` 基础上构建。

**建议**: 统一策略基类，让 `FactorScoreStrategy` 也继承 `BaseStrategy`。

---

### Bug #2 — `stock_scan.py` 第 259 行 `import signal` 未使用

**文件**: `backend/api/stock_scan.py:259`
**严重等级**: 🟡 **代码质量**

```python
def _run_single_scan(...):
    import signal      # ❌ 导入但未使用
    import threading   # ✅ 使用了
```

`signal` 模块在 Windows 上有功能限制（不支持 `SIGALRM`），而且代码中并没有使用它，超时控制用的是 `thread.join(timeout)`。

**修复**: 删除 `import signal` 行。

---

### Bug #3 — `stocks.py` K线接口硬编码 end_date 为 2025-12-31

**文件**: `backend/api/stocks.py:108-114`
**严重等级**: 🟡 **功能缺陷**

```python
df = ak.stock_zh_a_hist(symbol=code, period=ak_period,
    start_date=start_date or "20200101",
    end_date=end_date or "20251231",  # ❌ 硬编码为 2025-12-31
    adjust="qfq")
```

当前日期是 2026-07-06，如果用户不传 `end_date`，默认值是 `"20251231"`，意味着无法获取 2026 年的数据。

**修复**: 使用动态日期：
```python
from datetime import datetime
default_end = datetime.now().strftime("%Y%m%d")  # "20260706"
end_date=end_date or default_end,
```

---

### Bug #4 — `stocks.py` 分时图接口 avg_price 除零风险

**文件**: `backend/api/stocks.py:168`
**严重等级**: 🟡 **运行时崩溃风险**

```python
"avg_price": float(row.get("成交额", 0) or 0) / int(float(row.get("成交量", 0) or 1)),
```

虽然有 `or 1` 保护，但如果成交量为 0 且成交额也为 0，结果是 `0/1 = 0`，不会崩溃。但如果成交额为有效值而成交量为 0，会得到 `inf`。

**建议**: 明确处理：
```python
vol = int(float(row.get("成交量", 0) or 0))
amt = float(row.get("成交额", 0) or 0)
avg_price = round(amt / (vol * 100), 2) if vol > 0 else 0
```

---

## 三、代码质量问题（🟡 建议修复）

### Q1 — `data_loader.py` 和 `stock_names.py` 有重复的硬编码映射

两个文件中各有一份几乎相同的 40+ 只股票硬编码映射：
- `data_loader.py` → `_STOCK_NAME_HARDCODED` (L243-255)
- `stock_names.py` → `self._hardcoded` (L73-96)

**风险**: 两处维护不一致。

**建议**: 统一使用 `stock_names.py` 的 `StockNameCache` 作为唯一来源，`data_loader.py` 的旧映射标注为 `@deprecated` 或删除。

---

### Q2 — 选股扫描性能问题

`stock_scan.py` 中 `_run_single_scan()` 为每只股票启动一个 `Backtrader` 完整回测，每次都要：
1. 创建 `Cerebro` 引擎
2. 加载 K 线数据
3. 添加分析器
4. 运行回测

如果扫描 100 只股票，每个 10 秒超时，串行执行最长需要 1000 秒（~17 分钟）。

**建议**:
1. 短期：减小默认 `max_stocks` 为 20-30
2. 长期：实现并发扫描（`concurrent.futures.ThreadPoolExecutor`），可以 5-10 倍加速

---

### Q3 — 前端模拟进度条无实际意义

`StockScan.tsx:83-88`:
```typescript
const progressTimer = setInterval(() => {
    setProgress(p => Math.min(p + 5, 90))
}, 500)
```

进度条是固定每 500ms 加 5%，与实际扫描进度无关。

**建议**: 通过轮询后端进度接口或 WebSocket 推送真实进度，或至少标注"模拟进度"。

---

### Q4 — `StockDetail.tsx` 中多个 useEffect 重复绑定图表

第 31-35 行 `loadData` 依赖 `[symbol, klinePeriod]`，但第 89-129 行的 K 线图渲染 useEffect 又依赖 `[klineData, indicators]`，第 132-193 行的指标图又依赖 `[klineData, indicators, indicatorType]`。

这导致切换 K 线周期时：
1. `loadData` 重新请求
2. K 线图重新渲染
3. 指标图重新渲染

**风险**: 短时间内多次 `echarts.init` 和 `setOption` 可能导致图表闪烁。

**建议**: 使用 `echarts.dispose()` 确保旧图表实例清理，或使用 `useMemo` 优化。

---

### Q5 — `StockDetail.tsx` 未清理 iframe 加载

财务/公告/研报 Tab 使用东方财富 iframe 嵌入，但这些 iframe 始终存在于 DOM 中（只是通过 Tab 切换隐藏）。

**影响**: 页面首次加载时 3 个 iframe 都会发起网络请求，增加加载时间。

**建议**: 使用 `{activeTab === 'finance' && <iframe .../>}` 按需渲染。

---

### Q6 — 缺少 API 接口的超时处理

`frontend/src/api/index.ts` 中 `axios` 超时设为 `120000`（2 分钟），但 `frontend/src/services/api.ts` 中设为 `30000`（30 秒）。两个文件各自创建了独立的 axios 实例。

**风险**: 不同模块使用不同超时设置，行为不一致。

**建议**: 统一 API 配置，或删除冗余的 `services/api.ts`（该文件似乎是历史遗留）。

---

## 四、安全检查

| 检查项 | 结果 |
|--------|------|
| 注入攻击 | ✅ 无 eval/exec 动态执行 |
| XSS | ✅ iframe 使用东方财富官方域名，React 默认转义 |
| 敏感信息 | ✅ 无硬编码密码/Token |
| 文件路径遍历 | ✅ `Path` 对象安全使用 |

---

## 五、修复优先级总结

| 优先级 | 问题 | 文件 | 修复方案 |
|--------|------|------|----------|
| 🔴 P0 | FactorScoreStrategy 继承 bt.Strategy 而非 BaseStrategy | registry.py / factor_strategy.py | 统一继承 BaseStrategy |
| 🟡 P1 | K线 end_date 硬编码为 2025-12-31 | stocks.py:108 | 改为动态日期 |
| 🟡 P1 | 冗余 import signal | stock_scan.py:259 | 删除 |
| 🟡 P2 | 硬编码映射重复 | data_loader.py + stock_names.py | 统一来源 |
| 🟡 P2 | 选股扫描性能 | stock_scan.py | 减小默认值或引入并发 |
| 🟢 P3 | 前端模拟进度 | StockScan.tsx | 标注说明 |
| 🟢 P3 | iframe 按需加载 | StockDetail.tsx | 条件渲染 |
| 🟢 P3 | 重复 axios 实例 | api/index.ts + services/api.ts | 统一配置 |

---

## 六、总体评价

v3.0 升级在功能层面完整实现了 5 个需求，代码架构清晰，异常处理较为完善。主要问题集中在：

1. **策略基类不一致**（可能影响选股功能）
2. **硬编码日期**（影响 2026 年数据获取）
3. **选股扫描性能**（100 只股票串行扫描耗时过长）

建议优先修复 P0/P1 问题后再进行下一阶段开发。

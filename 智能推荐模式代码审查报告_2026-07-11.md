# 智能推荐模式代码审查报告

> 审查日期：2026-07-11（初版）→ 2026-07-11（问题 A 修复后复审）
> 审查对象：桌面需求文档《策略编辑器_智能推荐模式修复需求.md》第四节全部改动
> 审查结论：**需求已 100% 完成，问题 A 已修复，代码质量通过，可进入打包流程。**

---

## 一、需求完成度对照（验收标准逐项）

| # | 验收标准 | 状态 | 证据 |
|---|----------|------|------|
| 1 | 进入编辑器自动预填对应默认条件，无需手动点下拉 | ✅ | `VisualEditor.tsx` L65-88 ruleKey 切换 effect 的 `.catch` 调 `autoFillPreset(ruleKey)`；L91-96 presets 到位后补填 effect |
| 2 | 「智能推荐 💡」下拉仍保留，可手动重选/切换 | ✅ | `VisualEditor.tsx` L219-234 Select 下拉 + `handlePresetChange` L163-186 |
| 3 | 指标下拉分两段：推荐指标高亮 + 更多指标折叠 | ✅ | `ConditionCard.tsx` L39-64 两段式 `indicatorOptions`（推荐/更多）；antd Select 分组默认折叠 |
| 4 | 推荐指标与当前策略匹配 | ✅ | `registry.py` 四个策略各带 `recommended_indicators` 白名单，已验证接口返回正确 |
| 5 | 分组标题用语义色，无白块 | ✅ | `ConditionCard.tsx` L56/L60 用 `token.colorPrimary`/`token.colorTextTertiary` |
| 6 | dual_ma/bollinger 本期不预填，不报错 | ✅ | `registry.py` 仅 4 策略挂 `visual_defaults`，其余策略 `presets` 不含，前端 `recommendedIndicators` 为 undefined |
| 7 | 自动预填不破坏"有内容弹确认"逻辑 | ✅ | `handlePresetChange` L175 仍判断 `rule.items.length>0` 弹 Modal.confirm；自动预填走独立 `autoFillPreset` 不弹确认 |
| 8 | 不回退 404 修复、配色修复、between 校验 | ✅ | 以上逻辑（`findBetweenMissing` L100-118、语义色、404 category 切换）均保留，未改动 |

**结论：8/8 验收项全部满足。**

---

## 二、改动文件清单核对（与需求文档第五节一致）

| 文件 | 改动 | 审查结果 |
|------|------|----------|
| `backend/core/strategies/registry.py` | `StrategyInfo.preset_key` 字段、`_c()` 加 `recommended_indicators` 参数、`_VISUAL_DEFAULTS` 4 策略带白名单、循环挂载回填 | ✅ 正确 |
| `backend/api/visual_editor.py` | `/presets` 重构为 `{rule, name, recommended_indicators}` 平级 | ✅ 正确，已验证接口 |
| `frontend/src/api/index.ts` | `VisualPreset`/`VisualPresetsResponse` 类型 + `getVisualPresets()` | ✅ 类型正确 |
| `frontend/src/components/visual-editor/VisualEditor.tsx` | `autoFillPreset` + `autoFilledRef` + 三个 effect | ✅ 逻辑正确 |
| `frontend/src/components/visual-editor/ConditionCard.tsx` | 两段式指标下拉 | ✅ 正确 |
| `frontend/src/components/visual-editor/ConditionGroup.tsx` | 透传 `recommendedIndicators` | ✅ 正确 |
| `frontend/src/components/visual-editor/types.ts` | `applyPreset()` 复用（未改） | ✅ 无需改 |

---

## 三、构建与运行验证

| 验证项 | 命令 | 结果 |
|--------|------|------|
| 前端类型检查 | `tsc -b` | exitCode 0，无错误 |
| 前端生产构建 | `vite build` | 3681 模块，✓ built in 6.84s，无错误 |
| 后端接口 | `GET /api/visual/presets` | 返回 `{status:ok, data:{presets:{rsi,macd,kdj,ma_bullish}, names}}`，结构正确 |
| Lint | workspace diagnostics | 0 条诊断 |
| 后端数据 | `REGISTRY` 导入校验 | 4 策略 `preset_key` + `recommended_indicators` 正确 |

---

## 四、代码质量评价

### 优点
1. **去重机制设计合理**：`autoFilledRef`（useRef<Set>）防止切换策略时重复覆盖用户手动编辑，边界处理清晰。
2. **异步竞态已处理**：presets 异步到位后补一次 `autoFillPreset`（L91-96），避免 ruleKey 先于 presets 加载完成导致的漏填。
3. **两段式下拉用语义色**：符合深色主题约束，无写死浅色。
4. **复用而非重写**：`applyPreset`、`findBetweenMissing`、配色、404 修复均保留，未回退。
5. **模型约束尊重**：dual_ma/bollinger 因 `targetType=indicator` 不支持目标 line/period 而故意不纳入，与文档第六节一致。

### 发现的边界问题（低优先级，非阻塞）

**问题 A（已修复）：已保存空规则不会自动预填**
- 位置：`VisualEditor.tsx` L74-84（`.then` 分支）
- 原现象：用户此前保存过 `rule.items=[]` 的空规则，`loadVisualRule` 成功返回，`setRule(d.rule)` 设为空，不进 `.catch`，不触发 `autoFillPreset`。
- **修复方案**：在 `.then` 中判断加载到的规则是否为空，为空则调用 `autoFillPreset(ruleKey)`（不弹确认）。现三处触发路径一致：
  1. 新规则（`.catch`）：自动预填；
  2. 已保存空规则（`.then` 内判断）：自动预填；
  3. presets 异步到位后补填 effect：兜底。
- 验证：`tsc -b` exitCode 0、`vite build` 成功、lint 0。

**问题 B（信息性，非问题）：`autoFilledRef` 不随 ruleKey 变化重置**
- 设计意图：基于 `key=ruleKey` 做去重，跨策略切换各自独立记录，符合"不重复覆盖手动编辑"的语义，正确，无需修改。

### 未发现的问题
- 无类型错误、无未使用导入、无破坏性改动、无硬编码路径、无回退已修复逻辑。

---

## 五、审查结论（复审）

**需求已完成，问题 A 已修复，代码质量达标，建议直接进入打包流程（打包桌面版.cmd）。**

复审验证：
- `tsc -b`：exitCode 0
- `vite build`：3681 模块，✓ built in 6.86s
- Lint：0 诊断
- 全部边界路径（新规则 / 已保存空规则 / presets 异步补填）均触发自动预填且不弹确认。

无遗留问题，可打包发布。

---

*报告结束*

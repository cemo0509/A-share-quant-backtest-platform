# 审计整改计划

> 依据：《项目全面审计与升级路线图_2026-08-31.md》
> 制定日期：2026-08-31
> 原则：**先验证再修改**（审计已出现 1 次误判，不盲修）；按「决策伤害 × 修复成本」排序

---

## 零、已完成（第一批，commit 1170036）

已修复 9 类，均通过专项验证 + 15 策略端到端回归：

| 编号 | 问题 | 状态 |
|---|---|---|
| Q-01 | 夏普未年化 | ✅ 已加 `annualize=True` |
| Q-02 | 硬止损永不触发（`1-12.0=-11`） | ✅ 已 `/100` |
| Q-04 | 下单非整百股 | ✅ 修 4 个策略（审计只发现 1 个） |
| Q-05 | 年化按自然年开方 | ✅ 改用实际天数/365 |
| — | 夏普 `None`/`0` 混淆 | ✅ 改 `Optional[float]` |
| P0-3 | 假数据永久占坑（审计漏掉的衍生问题） | ✅ 模拟数据禁止落盘 |
| P0-4 | 缓存部分覆盖当完全覆盖 | ✅ `_covers()` + 增量合并 |
| P0-5 | 缓存键缺 adjust | ✅ 加复权标签 + 旧文件兼容 |
| P0-6 | 缺印花税/过户费/最低佣金 | ✅ `_AStockCommissionInfo` |
| F-03 | 涨跌配色双标准 | ✅ 统一 A 股「红涨绿跌」 |

---

## 一、第二批：低成本高收益（预计 1–2 小时）

> 逐条已验证源码，只对**确认为真**的问题动手

| # | 编号 | 任务 | 核实结果 | 修复方案 |
|---|---|---|---|---|
| 1.1 | **Q-03** | 动量策略单位混淆 | ✅ **属实** | `Momentum` 是价格差（元），`/100` 与 2% 阈值比较导致信号几乎不触发。改为真实收益率 `(close[0]-close[-n])/close[-n]` |
| 1.2 | **F-07** | 复权方式未暴露 | ✅ **属实** | `run_backtest` 无 `adjust` 参数，恒为 qfq。引擎加参数 → API 透传 → 前端加下拉框 |
| 1.3 | **D-11** | `sz688xxx` 科创板前缀误标 | ✅ 存在（审计判定低危/潜伏） | 8 只 688 应为 `sh`，当前调用链会剥前缀重新推断故未出错，顺手修正防未来踩坑 |
| 1.4 | **E-03** | 依赖零版本约束 | ✅ 属实 | `requirements.txt` 加上下界 pin；README 的 Node 16+ 修正为 18+（vite 6 要求） |
| 1.5 | **F-05** | Compare 佣金/滑点失效 | ❌ **不属实** | 后端 `backtest.py:107` 已正确透传，**不需要修**（记录以免重复排查） |
| 1.6 | **F-08** | `BacktestResultPage.tsx` 死代码 | ✅ 存在 | 未注册路由。需决策：接上 or 删除（见第四节） |

---

## 二、第三批：数据可靠性（预计 2–4 小时）

| # | 编号 | 任务 | 说明 |
|---|---|---|---|
| 2.1 | **D-09** | 打包时排除 `**/*.parquet` | 5 分钟。开发者本地若缓存了假数据会被直接分发 |
| 2.2 | **D-08** | 缓存 TTL + 清理机制 | 全市场预热 5000+ 文件，磁盘无上限增长 |
| 2.3 | **D-06** | 线程超时泄漏 `_BT_RUN_LOCK` | Python 无法外部终止线程，超时后僵尸线程持锁导致扫描假死。改进程池或线程内主动检查取消标志 |
| 2.4 | **E-02** | 全局异常改窄捕获 | `net_errors.py` 文本匹配会把带「timeout/网络」字样的 TypeError/KeyError 伪装成数据源问题且不记 traceback。改：仅在 HTTP/akshare 调用点捕获 `requests.RequestException`，抛自定义 `UpstreamError` |
| 2.5 | **Q-08** | 指标库扩容 | 现仅 8 字段，补波动率 / Sortino / Calmar / Alpha / Beta / 回撤修复期 / 换手率 |

---

## 三、第四批：大工程（每项约 1 天，需单独排期）

> 依赖顺序：3.3 持久化 → 3.4 基准对比 → 3.2 样本外验证

| # | 编号 | 任务 | 说明 | 依赖 |
|---|---|---|---|---|
| 3.1 | **P0-7** | 涨跌停 + 停牌建模 | 追涨停/突破类策略目前全是虚假成交，回测结果失去参考价值 | — |
| 3.2 | **P0-8** | 参数优化样本外验证 | 网格 2000 组直接按夏普排序 = 过拟合。需样本内/外切分 + 差距告警 + 敏感性热力图 | — |
| 3.3 | **P0-9** | 回测结果持久化（SQLite） | 结果现只存 zustand，刷新即丢。**是复盘/对比/归因的前置依赖** | — |
| 3.4 | **P0-10** | 基准对比（买入持有 + 沪深300） | 投入产出比最高：策略年化 20% 但同期买入持有 25% 即为失败，当前完全看不出 | — |
| 3.5 | **Q-06/Q-07** | 接 `position_sizer` + T+1 约束 | 仓位管理代码已写完但未接线（低垂果实）；T+1 校验缺失 | — |
| 3.6 | **E-01** | 建测试体系 | 现仅 3 条 pytest。目标不是覆盖率数字，而是「改坏会红」——金标准回归测试 20 条 | 3.1–3.5 之后 |

---

## 四、需要你决策的事项

这些我不能替你决定：

| 事项 | 选项 A | 选项 B | 决策 |
|---|---|---|---|
| ~~`BacktestResultPage.tsx` 死代码~~ | 接上路由 | 删除 | ✅ **已删除**（决策1） |
| ~~可视化编辑器 codegen~~ | 补 codegen 闭环 | 标注「草稿本」 | ✅ **选 A，已实现**（决策2） |
| ~~`position_sizer.py` / `exit_rules.py`（写完未接线）~~ | 立即接入引擎 | 维持现状 | ✅ **选 A，已接入**（决策3） |
| ~~打包体积（安装后 600MB–1GB）~~ | 接受 | 精简依赖 | ✅ **接受现状**（决策4） |

> **四项决策已全部完成**（2026-09-01）。

---

## 五、明确不做（审计第七部分，认同）

虚拟列表 / K8s / 微服务 / Celery / Redis / 重写分层 / DDD / GraphQL / 追求覆盖率数字 / 自建行情数据库 / 多用户鉴权 / i18n / 自动更新 / 社区·荐股·资讯流·广告。

理由：单机桌面应用，用户数 = 1，注意力是最稀缺资源。做这些是永久性运维负债，且会让你从「决策者」退化为「信息消费者」。

---

## 六、执行原则（本次审计的教训）

1. **先验证再修改**：上一份 WorkBuddy 诊断核心论断（attrs 丢失）经实测证伪；本次审计虽准确，但 F-05 仍误判。**不实测不盲修**。
2. **警惕衍生问题**：审计说「假数据洗白」时只盯标记是否丢失，漏了更严重的「假数据永久占坑」。**标记正确 ≠ 数据正确**。
3. **保留已做对的部分**：无未来函数、市价单次日开盘成交、扫描进度断点续传、BOLL 用 `ddof=0`、防御性编码密度——这些修改时必须保留。
4. **每批结束必须回归**：15 策略端到端跑通 + pytest + tsc，确认没改坏。

---

## 七、当前进度

- [x] 第一批：P0 数值与缓存正确性（已完成，commit `1170036`）
- [x] 第二批：低成本高收益（已完成，commit `79bb7cc`）
  - 1.1 Q-03 动量单位 ✅ 修（附：`momentum` 策略交易数 25→30，信号恢复正常）
  - 1.2 F-07 复权透传 ✅ 修（引擎→API→前端全链路 + 校验器）
  - 1.3 D-11 `sz688` 前缀 ✅ 修（8 只改 `sh`）
  - 1.4 E-03 依赖约束 ✅ 修（10 项加上下界）
  - 1.5 F-05 Compare 佣金 ❌ **经核实不属实**，后端已正确透传，无需修
  - 1.6 F-08 死代码 → 移至第四节待决策
- [x] 第三批：数据可靠性（已完成，commit `782dc70`）
  - 2.1 D-09 打包排除 parquet ✅（防开发者本地假数据被分发）
  - 2.2 D-08 缓存年龄追踪 + 过期清理 ✅（`age_days`/`stale` 字段 + `DELETE /cache/stale` + 前端按钮）
  - 2.3 D-06 回测锁带超时 ✅（`_BT_LOCK_TIMEOUT=30s`，僵尸线程不再导致整轮假死）
  - 2.4 E-02 异常判定收窄 ✅（移除 `timeout`/`网络` 等宽泛词；`TimeoutError` 不再算网络错误）
  - 2.5 Q-08 指标库扩容 ✅（波动率/Sortino/Calmar/回撤修复期/年化交易次数）
- [x] 第四批 1/6：**P0-10 基准对比**（已完成，commit `7cfe3be`）
- [x] 第四批 2/6：**P0-7 涨跌停/停牌建模**（已完成，commit `69860e2`）
- [x] 第四批 3/6：**P0-9 回测结果持久化（SQLite）**（已完成，commit `edd55d6`）
- [x] 第四批 4/6：**P0-8 参数优化样本外验证**（已完成，commit `11f25da`）
- [x] 第四批 5/6：**Q-07 T+1 约束**（已完成）
  - 先验证再修改：15 策略实测**现状零违规**（Market 单次日开盘成交天然满足 T+1）
  - 发现真实缺口：**做空未被禁止**（无持仓 `sell()` 实测开出 -1000 股负持仓）
  - 引擎层动态包装 `_apply_astock_rules`：Close 单/cheat 当日成交受 T+1 冻结约束；
    做空直接拦截；对预置策略零误伤（防线计数全 0）
  - 防线计数透出 `constraints` 字段，结果页拦截告警
  - 专项 18/18 PASS（恶意 Close 单 10 次 T+1 拦截 + 47 次做空裁剪）
- [ ] 第四批 6/6：E-01 测试体系

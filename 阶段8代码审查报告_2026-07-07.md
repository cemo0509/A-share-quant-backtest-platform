# 阶段8 代码审查报告

> 审查日期：2026-07-07  
> 审查范围：全项目（backend/api、backend/core、backend/models、frontend、electron）  
> 审查基准：代码安全性、异常处理、性能、一致性、最佳实践

---

## 总体评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 代码安全性 | B+ | 自定义代码执行已有安全审计，但部分接口仍存风险 |
| 异常处理 | B | 多数接口有 try-except，但异常信息泄露问题存在 |
| 代码一致性 | B+ | 整体风格一致，部分模块存在冗余/不一致 |
| 性能 | B | 基本可接受，选股扫描有大内存风险 |
| 测试覆盖 | C | 仅有少量测试脚本，无单元测试 |

---

## 高风险问题（H）

### H1. 异常信息直接暴露给客户端 — 多处

**文件**: `backend/api/data.py:31`, `backend/api/export.py:33,71`, `backend/api/market.py:48,75`, `backend/api/optimize.py:160`, `backend/api/stocks.py:137,178,219,262`, `backend/api/stock_scan.py:163`, `backend/api/trading.py:82`

**问题**: 大量路由在异常处理中直接将 `str(e)` 返回给客户端（`raise HTTPException(status_code=500, detail=str(e))`），可能泄露内部路径、数据库结构等敏感信息。

```python
# 当前代码
except Exception as e:
    raise HTTPException(status_code=500, detail=str(e))
```

**建议**: 
- 对500错误统一使用通用消息，将真实错误记录到日志
- 仅400错误可返回用户友好的详细错误

**影响文件清单**:
- `backend/api/data.py` 第31行
- `backend/api/export.py` 第33、71行
- `backend/api/market.py` 第48、75行
- `backend/api/optimize.py` 第160行
- `backend/api/stocks.py` 第137、178、219、262行
- `backend/api/stock_scan.py` 第163行
- `backend/api/trading.py` 第82行

---

### H2. 选股扫描无并发上限控制 — 内存风险

**文件**: `backend/api/stock_scan.py:180-193`

**问题**: `_run_concurrent_scan` 使用 `ThreadPoolExecutor(max_workers=8)` 但一次性提交所有symbols任务到future_map，当扫描全市场股票时（如5000+只），会同时创建5000+个Future对象和后台线程，可能导致内存飙升。

```python
# 第180-193行
with ThreadPoolExecutor(max_workers=max_workers) as executor:
    future_map = {}
    for symbol in symbols:  # 可能5000+只
        future = executor.submit(...)
        future_map[future] = symbol  # 内存中同时持有所有future
```

**建议**: 使用 `ThreadPoolExecutor` 的 `map` 方法或分批提交任务（如每批100个），或使用 `Semaphore` 控制同时处理的任务数。

---

### H3. CORS 配置过于宽松

**文件**: `backend/main.py:26-38`

**问题**: 
1. `allow_origin_regex=r"^(https?://localhost:\d+|null)$"` 允许任意 localhost 端口，存在 SSRF 风险
2. `allow_methods=["*"]` 和 `allow_headers=["*"]` 过于宽松

```python
allow_origin_regex=r"^(https?://localhost:\d+|null)$"
allow_methods=["*"]
allow_headers=["*"]
```

**建议**: 
- `allow_origin_regex` 限制为已知端口（如 `5173,4173,8000`）
- 生产环境下应使用具体的 `allow_origins` 列表

---

### H4. 回测比较接口无并发超时保护

**文件**: `backend/api/backtest.py:76-114`

**问题**: `compare_strategies` 串行执行多个策略回测，每个都可能耗时30秒以上。如果比较10个策略，总耗时可能超过5分钟，且无任何超时或进度反馈机制。

```python
for strategy_key in req.strategies:
    result = run_backtest(...)  # 串行，无超时
```

**建议**: 
- 添加总超时控制（如300秒）
- 使用 `ThreadPoolExecutor` 并发执行
- 返回部分结果而非全有或全无

---

## 中风险问题（M）

### M1. `stock_scan.py` 中重复 `import threading` 和 `import time`

**文件**: `backend/api/stock_scan.py:14,249,468`

**问题**: `threading` 在文件顶部（第14行）、`_run_concurrent_scan` 函数内（第249行）、`_run_single_scan` 函数内（第468行）共 import 了3次。`time` 也有类似重复导入。

```python
# 第14行（顶部）
import threading
# 第249行
import threading  # 重复
# 第468行
import threading  # 重复
```

**建议**: 移除函数内的重复导入，统一使用顶部的 import。

---

### M2. `engine.py` 中 `_safe_convert` 方法与 `analyzers.py` 中同名方法重复

**文件**: `backend/core/engine.py:42-56`, `backend/core/analyzers.py:40-53`

**问题**: 两个文件中存在几乎完全相同的 `_safe_convert` 静态方法（numpy类型安全转换），代码重复。

**建议**: 提取为 `backend/utils/serialization.py` 公共工具函数。

---

### M3. `custom_manager.py` 中 `load_strategy_from_code` 使用临时文件方式

**文件**: `backend/core/strategies/custom_manager.py:260-317`

**问题**: 从代码字符串加载策略时，先写入临时文件再 `importlib.util.spec_from_file_location` 加载，增加了IO开销和安全风险（临时文件可能被其他进程读取）。

**建议**: 可以考虑使用 `importlib.util.spec_from_loader` + 自定义 Loader 直接从字符串加载，避免写磁盘。但当前实现已有充分的安全检查（`_validate_code_security`），风险较低。

---

### M4. `data_loader.py` 中全局变量缺乏线程安全保护

**文件**: `backend/core/data_loader.py:48-54`

**问题**: 
- `_REALTIME_CACHE` 字典在多线程环境下被读写，但没有锁保护
- `_STOCK_NAME_MAP` 虽有 `_STOCK_NAME_MAP_LOCK`，但 `_fetch_realtime_from_akshare` 中直接修改 `_STOCK_NAME_MAP`（第418-425行）没有使用锁

```python
# 第418行
global _STOCK_NAME_MAP
for _, row in df.iterrows():
    _STOCK_NAME_MAP[code] = name  # 无锁写入
```

**建议**: 对 `_REALTIME_CACHE` 和 `_STOCK_NAME_MAP` 的写操作加锁保护。

---

### M5. `main.py` 中 `_is_packaged` 检测方式不可靠

**文件**: `backend/main.py:25`

**问题**: 
```python
_is_packaged = "resources" in str(Path(__file__).resolve()).lower()
```
用路径中是否包含 "resources" 来判断是否在打包环境中，这种方式在开发目录也包含 "resources" 时会误判。

**建议**: 使用环境变量 `APP_IS_PACKAGED=true` 由 Electron 进程传入，或检查 `sys.frozen` / `getattr(sys, 'frozen', False)`。

---

### M6. `trading.py` 中 `_save_trading_state` 静默吞异常

**文件**: `backend/api/trading.py:26-32`

**问题**: 
```python
def _save_trading_state(simulator: TradingSimulator):
    try:
        ...
    except Exception:
        pass  # 静默失败，不影响主流程
```
状态持久化失败被完全忽略，如果磁盘满或权限不足，用户数据会丢失且无任何提示。

**建议**: 至少记录一条 warning 日志。

---

### M7. `stocks.py` 中 `get_intraday_data` 使用了已弃用的 `datetime.utcnow()`

**文件**: `backend/api/stocks.py:141-178`

**问题**: 在非交易时段，`get_intraday_data` 接口返回的数据可能为空或过时。同时 `ak.stock_zh_a_hist_min_em` 的返回值在周末/节假日可能为空DataFrame，前端可能显示异常。

**建议**: 添加交易日判断，非交易时段返回友好提示。

---

### M8. 前端 `Trading.tsx` 中盈亏颜色逻辑反转

**文件**: `frontend/src/pages/Trading.tsx:153`

**问题**: 
```tsx
render: (profit: number) => (
    <span style={{ color: profit >= 0 ? '#f5222d' : '#52c41a' }}>
        {profit.toFixed(2)}
    </span>
),
```
在中国A股市场，红色(#f5222d)通常表示上涨/盈利，绿色(#52c41a)表示下跌/亏损。当前代码中 `profit >= 0` 显示红色是正确的（中国习惯），但 `profit < 0` 显示绿色也符合中国习惯。**经确认，此处逻辑正确**。但建议添加注释说明这是中国股市的颜色惯例。

**状态**: 无需修改，建议添加注释。

---

### M9. `stock_scan.py` 中 SSE 端点 `event_stream` 缺乏异常保护

**文件**: `backend/api/stock_scan.py:262-308`

**问题**: SSE 事件流中的 while 循环如果发生异常（如 `_SCAN_PROGRESS` 字典被意外修改），生成器会静默中断，前端将收到不完整的事件流但不会感知到错误。

**建议**: 在 `event_stream` 内部添加 try-except，异常时发送 error 事件并关闭连接。

---

## 低风险问题（L）

### L1. `custom_manager.py` 中 `import logging` 放在文件末尾

**文件**: `backend/core/strategies/custom_manager.py:256-257`

**问题**: 
```python
import logging
_cm_logger = logging.getLogger("custom_manager")
```
import 语句和变量定义放在类/函数定义之后（文件末尾），不符合 PEP8 规范。虽然Python允许这样做，但降低了可读性。

**建议**: 将 `import logging` 移到文件顶部与其他 import 合并。

---

### L2. `backtest.py` 中 `run_code` 函数参数未使用 `params`

**文件**: `backend/api/backtest.py:46-71`

**问题**: `BacktestCodeRequest` 模型中没有 `params` 字段，但 `run_backtest` 函数签名中 `params` 默认为空字典。这导致自定义代码回测不支持参数传入。

**建议**: 在 `BacktestCodeRequest` 中添加 `params` 字段（可选）。

---

### L3. `schemas.py` 中 `BacktestCodeRequest.code` 的 `max_length` 校验可能被绕过

**文件**: `backend/models/schemas.py:71`

**问题**: 
```python
code: str = Field(..., max_length=500000, description="策略 Python 代码")
```
Pydantic 的 `max_length` 是针对字符串长度的。但 `custom_manager.py` 中的 `_validate_code_security` 也做了 `MAX_CODE_LENGTH = 500_000` 检查，两者重复但数值一致。如果有大量注释或空格，实际有效代码可能没那么多但仍被拒绝。

**建议**: 统一两处的长度限制常量来源，或仅在 `_validate_code_security` 中做一次检查。

---

### L4. `frontend/src/services/api.ts` 导出冗余

**文件**: `frontend/src/services/api.ts:28`

**问题**: 
```typescript
export { default } from '../api'
```
同时作为 default export 和 named export 导出，可能造成使用者困惑。

**建议**: 明确文档说明使用方式，或移除 `default` 导出。

---

### L5. `Dashboard.tsx` 中的 `features` 描述与实际功能不完全匹配

**文件**: `frontend/src/pages/Dashboard.tsx:11`

**问题**: 首页描述中提到"支持日K/分钟K回测"，但当前 `Backtest` 页面只有日期选择器，没有分钟级K线周期选择。

**建议**: 更新描述或添加分钟级K线支持。

---

### L6. 缺少 `requirements.txt` 中的版本锁定

**文件**: `backend/requirements.txt`

**问题**: 未检查到版本锁定内容，如果依赖使用 `>=` 或不指定版本，可能导致不同环境下行为不一致。

**建议**: 使用 `pip freeze > requirements.txt` 生成带精确版本的依赖列表，或使用 `poetry`/`pipenv` 管理。

---

### L7. `optimize.py` 中 `import signal` 未使用

**文件**: `backend/api/optimize.py:50`

**问题**: 
```python
import signal  # 导入但未使用
```
第50行导入了 `signal` 模块但在代码中从未使用。

**建议**: 移除未使用的导入。

---

### L8. `position_sizer.py` 中 `Enum` 值与字符串混用

**文件**: `backend/core/position_sizer.py:65-69`

**问题**: `calc_position_size` 函数中使用 `SignalGrade.A.value` 获取字符串值来构建字典键，然后又用传入的字符串参数去查找。这种设计需要确保传入的字符串严格匹配枚举值，但没有做规范化处理。

```python
grade_mult = {
    SignalGrade.A.value: 1.2,
    ...
}.get(signal_grade, 1.0)
```
虽然有 `signal_grade.upper()` 处理，但如果传入 `"a"` 而不是 `"A"`，仍能匹配。

**状态**: 当前代码已做 `signal_grade.upper()` 处理，风险较低。

---

### L9. `electron/main.js` 中打包环境路径检测存在边界情况

**文件**: `electron/main.js:248-260`

**问题**: `possiblePaths` 列表中包含 `app.asar` 路径，但在开发环境中 asar 文件可能不存在。`require('fs').existsSync` 对 asar 内的路径支持可能有限。

**建议**: 添加对 asar 路径的专门处理逻辑。

---

## 建议修复优先级

| 优先级 | 编号 | 问题 | 预计工时 |
|--------|------|------|----------|
| P0 | H1 | 异常信息泄露 | 2h |
| P0 | H3 | CORS 配置过于宽松 | 0.5h |
| P1 | H2 | 选股扫描无并发控制 | 2h |
| P1 | H4 | 策略比较无超时保护 | 1.5h |
| P1 | M4 | 全局变量线程安全 | 1h |
| P2 | M1 | 重复import清理 | 0.5h |
| P2 | M2 | _safe_convert 代码重复 | 1h |
| P2 | M6 | 状态持久化静默失败 | 0.5h |
| P2 | M9 | SSE事件流异常保护 | 0.5h |
| P3 | L1-L9 | 低风险问题 | 2h |

---

## 总结

本次审查共发现 **4个高风险问题、9个中风险问题、9个低风险问题**，合计 **22个问题**。

相比阶段7审查（19个问题），阶段8的代码质量整体有所提升，主要新增问题集中在：
1. **异常信息安全**：多处路由直接暴露 `str(e)` 给客户端（阶段7未完全覆盖）
2. **并发/内存风险**：选股扫描和策略比较的并发控制不足
3. **线程安全**：全局缓存的读写操作需要更好的锁保护

建议优先修复 H1（异常信息泄露）和 H3（CORS配置），这两项直接影响系统安全性。

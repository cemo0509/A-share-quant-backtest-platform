# 项目文件清理记录

> 清理日期：2026-07-11
> 清理范围：仓库根目录的无用临时文件、废弃打包目录、一次性脚本与日志
> 清理依据：未被任何 `.cmd` / `.md` / `.bat` / `.ps1` 引用，或功能已合入主代码、或使命已完成

---

## 一、删除清单

### 1. 废弃打包目录（旧试验残留，约 39000+ 文件）
| 目录 | 说明 |
|------|------|
| `dist_build_new/` | 旧打包试验，未被 `electron-builder.yml` 引用（output 为 `dist_build`） |
| `dist_build2/` | 同上 |
| `dist_build3/` | 同上 |

### 2. 根 `dist/`（1.1.1 旧安装包残留）
| 文件/目录 | 说明 |
|-----------|------|
| `dist/win-unpacked/` | 旧目录模式产物（1.1.1） |
| `dist/A股量化回测平台 Setup 1.1.1.exe` | 旧版安装包，已被 `dist_build/` 的 1.1.2 取代 |
| `dist/A股量化回测平台 Setup 1.1.1.exe.blockmap` | 旧版 blockmap |
| `dist/builder-debug.yml`、`dist/builder-effective-config.yaml` | 旧打包调试配置 |

### 3. 一次性脚本（功能已合入或使命已完成）
| 文件 | 删除原因 |
|------|----------|
| `add_strategy_templates.js` | 策略模板已合入 `frontend/src/pages/StrategyEditor.tsx` |
| `verify_charts.js` | playwright 一次性图表渲染验证 |
| `verify_stock.png` | 上述脚本截图产物 |
| `check_kline.ps1` | 读 `test_kline.json` 的一次性检查 |
| `test_kline.json` | 上述脚本依赖数据 |
| `findlock.ps1` | 查安装包被谁占用，使命已完成 |
| `test_echarts.html` | 测试页面 |
| `test_indicators.json` | 测试数据 |
| `test_strategies.py` | 阶段2遗留测试，无脚本实际调用 |
| `test_phase2.py` | 同上 |
| `test_phase2_simple.py` | 同上 |
| `backtest_phase2.py` | 同上 |
| `test_frontend.bat` | `npm start` 旧启动方式，已被 `启动测试版.cmd`/`启动程序.cmd` 取代 |
| `validate_installer.py` | 安装包验证脚本，安装包已生成且 `install_test_1.1.2/` 含验证产物 |

### 4. 临时日志
| 文件 | 说明 |
|------|------|
| `pack_fe.log` | 前端打包临时日志 |
| `pack_dir.log` | 目录模式打包临时日志 |
| `_vite_build.log` | vite build 临时日志 |
| `_p.out.log` / `_p.err.log` | 后端测试启动临时日志 |
| `dist_build_log.txt.err` / `dist_build_log.txt.out` | 旧打包日志 |
| `packaging_log.txt` | 旧打包日志 |

---

## 二、保留项（确认仍有用，未删除）

| 文件/目录 | 保留原因 |
|-----------|----------|
| `dist_build/` | 活跃打包输出，含 **A股量化回测平台 Setup 1.1.2.exe** |
| `test_backend.py` + `测试后端启动.cmd` | 仍被引用，作后端启动入口 |
| `手动打包.cmd`、`启动测试版.cmd`、`启动程序.cmd` | 启动 / 备用打包脚本 |
| `build/` | `electron-builder.yml` 的 `buildResources`，配置引用，必须保留 |
| `install_test_1.1.2/`、`v1.0发布包/` | 安装测试产物，非巨大，保守保留 |
| 所有 `阶段*.md`、`WorkBuddy同步说明.md`、`智能推荐模式代码审查报告_2026-07-11.md` 等文档 | 项目文档，保留 |

---

## 三、清理效果

- 根目录条目：70+ → 约 50 项
- 释放空间：3 个旧打包目录（各约 500MB 级）+ 1.1.1 旧安装包 + 各类临时文件
- 活跃安装包与源码未受影响，`dist_build\A股量化回测平台 Setup 1.1.2.exe` 验证存在

---

*记录结束*
